# Optional local APNs relay configuration for `scripts/tron dev`.
# Copy to `.env.local`, keep the file private, and configure both values.
TRON_RELAY_URL=https://relay.example.invalid
TRON_RELAY_SECRET=replace-with-a-random-shared-secret
