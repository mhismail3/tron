// Prod.xcconfig - Production variant settings

// Standard bundle ID
PRODUCT_BUNDLE_IDENTIFIER = com.tron.mobile

// Standard app icon
ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon

// No additional compile-time flags — prod is the baseline (not DEBUG, not BETA)
