/**
 * @fileoverview Configuration management
 *
 * Provides:
 * - Default configuration values
 * - Feature flags
 * - Configuration loading
 *
 * @migration This consolidates settings from settings/ folder.
 */

// Re-export from settings during migration
export * from '../../settings/index.js';
