/**
 * @fileoverview Turn Handler Constants
 *
 * Constants for tool event handler and related turn processing.
 */

/** Threshold for storing tool results in blob storage (2KB) */
export const BLOB_STORAGE_THRESHOLD = 2 * 1024;

/** Maximum size for tool result in context (10KB) */
export const MAX_TOOL_RESULT_SIZE = 10 * 1024;
