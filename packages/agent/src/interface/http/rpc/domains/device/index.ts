/**
 * @fileoverview Device domain - Device registration
 *
 * Handles device registration and unregistration for push notifications.
 */

// Re-export handler factory
export { getDeviceHandlers } from '@interface/rpc/handlers/device.handler.js';

// Note: Device types are defined inline in the handler
