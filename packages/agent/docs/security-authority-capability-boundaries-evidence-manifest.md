# Security Authority Capability Boundaries Evidence Manifest

Status: **active**

Current score: **95/100**

Scorecard:
[`security-authority-capability-boundaries-scorecard.md`](security-authority-capability-boundaries-scorecard.md)

Inventory:
[`security-authority-capability-boundaries-inventory.md`](security-authority-capability-boundaries-inventory.md)
and
[`security-authority-capability-boundaries-inventory.tsv`](security-authority-capability-boundaries-inventory.tsv)

## Row Ledger

| Row | Status | Evidence | Verification | Closure | Checkpoint |
|---|---|---|---|---|---|
| SACB-0 | passed_after_fix | Added scorecard, evidence manifest, inventory summary, machine-readable TSV header/seed rows, invariant target, README links, local/GitHub CI closeout wiring, and cross-campaign PCC/HRA rows for new SACB artifacts. Initial findings record the public transport context trust gap and grant derivation file-root narrowing gap discovered during baseline source inspection. | SACB target passed with 6 tests; PCC inventory target passed with 16 tests; HRA inventory target passed with 35 tests; rustfmt check and whitespace checks passed. | Closed for harness. Open rows SACB-1 through SACB-10 remain explicitly pending in the scorecard. | SACB-0 campaign harness checkpoint |
| SACB-1 | passed_after_fix | Replaced the SACB-0 seed TSV with marker-derived inventory coverage across tracked server, iOS, Mac, script, workflow, docs, tests, and TSV evidence surfaces for public transport, authority grants, runtime metadata, primitive execution, external workers, secret storage, pairing lifecycle, and static gates; current coverage is 603 structured rows. Added static marker coverage so every tracked security-marker file must have a SACB row. | SACB target passed with the marker coverage and boundary-class guards. | Closed for whole-repo inventory. Later rows must keep the inventory current as they delete unsafe fields or add focused tests. | SACB-1 boundary inventory checkpoint |
| SACB-2 | passed_after_fix | Added worker route tests in `packages/agent/src/app/bootstrap/server.rs` for missing bearer and loopback bearer upgrade, plus a direct worker peer guard test for non-loopback `403` rejection. Added SACB static route/auth guards for `/engine`, `/engine/workers`, `ws_auth_gate`, strict bearer parsing, and loopback worker `ConnectInfo<SocketAddr>` enforcement. Existing auth tests already cover missing, wrong, rotated, wrong-scheme, trailing-whitespace, malformed, and constant-time bearer behavior. | Server bootstrap target passed with 24 tests; SACB invariant target passed with 9 tests. | Closed for public route/auth/loopback boundary. Later SACB-3 owns public context trust. | SACB-2 public transport boundary checkpoint |
| SACB-3 | passed_after_fix | Removed public `authorityScopes` and `runtimeMetadata` from `/engine` wire context and the transport-neutral `EngineTransportContext`, deleted the copy loops that inserted caller context scopes/metadata into `CausalContext`, removed silent top-level `authorityScopes` stripping, added DTO tests that reject those fields, and added static SACB guards for the deletion. README documents public context as identity/correlation-only. | Engine transport unit target passed with 16 tests; SACB invariant target passed with 10 tests; formatting and whitespace checks passed. | Closed for public context trust. Later SACB-4 owns grant derivation and bootstrap grant proof. | SACB-3 public context trust checkpoint |
| SACB-4 | passed_after_fix | Added `packages/agent/src/engine/authority/grants/paths.rs` as the shared canonical path-containment helper, changed grant derivation to canonicalize and normalize child file roots before comparing to canonical parent roots, removed the raw `root.starts_with(parent)` boundary, added prefix-sibling and unresolved parent-component escape regressions, and added bootstrap root-grant proof for explicit wildcard policy plus `engine.bootstrap` provenance. Updated PCC, TPC, HRA, TMB, SOL, and SACB inventories for the new helper. | Authority grant target passed with 9 tests; SACB invariant target passed with 11 tests; PCC, TPC, HRA, TMB, and SOL inventory targets passed with focused coverage; formatting and whitespace checks passed. | Closed for grant derivation, file roots, network policy/budget proof, and bootstrap grant proof. Later SACB-5 owns catalog visibility/direct invocation. | SACB-4 authority grant boundary checkpoint |
| SACB-5 | passed_after_fix | Tightened internal catalog visibility so `engine.internal.invoke` is accepted only from trusted runtime actor kinds, changed hidden agent prompt/apply delegation to run under `system:agent-runtime` with `engine-system` authority instead of cloning public client causality, added direct `engine::invoke` tests proving public client contexts cannot reach internal/admin/worker-only targets and raw public internal scope is denied, and added public transport/static guards proving `/engine` does not mint the internal scope. | Policy target passed with 2 tests; prompt helper target passed with 1 test; engine meta invocation target passed with 12 tests; transport target passed with 17 tests; SACB invariant target passed with 12 tests; formatting passed. | Closed for catalog visibility and direct invocation. Later SACB-6 owns `capability::execute` least privilege. | SACB-5 catalog visibility/direct invocation checkpoint |
| SACB-6 | passed_after_fix | Agent capability execution now derives a per-call child grant from `agent-capability-runtime` before invoking `capability::execute`; the child grant carries the exact target function, no namespace authority, state read/write support, canonical working-directory file root, `networkPolicy: none`, and non-delegable provenance. The execute worker rejects bootstrap grants and public/user contexts, requires trusted working-directory runtime metadata instead of falling back to process cwd, authorizes relative file paths against the canonical root, denies system-scoped state, requires current-session context for trace/log/replay reads, and requires `process_run` to inspect an active grant with `networkPolicy none` before running under the network-denial sandbox. | Capability operation tests passed; capability invocation executor tests passed; primitive trace execution tests passed with public-client/bootstrap/system-state/network-policy regressions; registration smoke passed with derived grant and trusted working-directory metadata; SACB invariant target passed with the new static guards; formatting and whitespace checks passed. | Closed for primitive execute least privilege. Later SACB-7 owns external worker protocol isolation. | SACB-6 capability execute least-privilege checkpoint |
| SACB-7 | passed_after_fix | Worker protocol tokens now default to namespace-specific stream selectors and expose session/workspace binding builders. Worker hello rejects unsupported auth policy, non-external worker kinds, unbound session/workspace visible workers, wildcard selectors, unknown/inactive/stale grants, and mismatched grant subject bindings. Namespace validation uses strict segment/prefix matching instead of substring matching. Function metadata, trigger ids/targets, trigger authority grant, stream visibility, stream topic, and stream session/workspace scope are checked against the accepted connection and scoped token. Socket result handling remains per-connection pending-map owned and disconnect/backpressure failures drain pending invocations. | External worker runtime tests passed with 21 tests, including scoped token binding, wildcard selector, substring namespace, cross-worker trigger target, stream session, and stream topic denial regressions; restart chaos passed for durable disconnected worker hydration; trigger runtime passed; transport external-worker socket tests passed; SACB invariant target passed with static guards; formatting and whitespace checks passed. | Closed for external worker protocol isolation. Later SACB-8 owns secrets, token storage, redaction, and provider credential custody. | SACB-7 external worker protocol isolation checkpoint |
| SACB-8 | passed_after_fix | Provider credentials and the WebSocket bearer token remain owned by `domains/auth/credentials/storage`: writers load existing storage through the malformed-file-preserving write helper and persist atomically through a `0o600` temp file plus `sync_all`/rename. Bearer-token creation and rotation continue to materialize only the pristine `{}` install sentinel and refuse malformed non-empty files. Server-side redaction now covers JSON auth fields, Swift/Rust debug-description auth fields, and unquoted OAuth/API-key key-value forms, and client log ingestion applies that redactor before durable `logs` storage. iOS source guards prove `PairedServer` has no token/auth/API-key fields, paired-server bearer tokens stay in `PairedServerTokenStore`/Keychain, token resolution reads only active server metadata from UserDefaults, iOS logging redacts before buffering/OS logging, and network diagnostics report only authorization presence. Mac diagnostics redaction now matches iOS for camelCase and Swift-description auth fields. | Auth storage/onboarding tests passed for secure mode and malformed-file preservation; event-store redaction/log ingestion tests passed with bearer/API/OAuth regressions; iOS source guards and diagnostics/token-store tests passed after restoring the canonical diagnostics-loop doc phrase; Mac diagnostics redaction tests passed; the Rust SACB static guard verified Mac/iOS redaction parity markers and Keychain/source-custody markers; formatting and whitespace checks passed. | Closed for secrets, token storage, redaction, `auth.json` permissions, and provider credential custody. Later SACB-9 owns pairing lifecycle behavior after the token is intentionally presented to a user. | SACB-8 secret custody and redaction checkpoint |
| SACB-9 | passed_after_fix | iOS QR/deep-link parsing and manual pairing validation now share `PairingHostValidator`, which accepts only bare DNS, IPv4, or unbracketed IPv6 hosts and rejects full URLs, path/query/userinfo fragments, bracketed IPv6, malformed IPv4, and malformed DNS labels before a WebSocket probe or local persistence. Mac `PairingURLBuilder` now mirrors the same host and `1...65535` port contract so QR payloads cannot be broader than iOS accepts. `PairingPersistor` canonicalizes direct payloads, preconditions invalid internal host payloads instead of carrying fallback behavior, and exposes an explicit rollback plan that restores the previous token for re-pair failures or removes the candidate token for new-server failures. `DependencyContainer.forgetPairedServer` now deletes the Keychain token before removing paired-server metadata and throws on deletion failure; Settings surfaces that failure instead of silently forgetting metadata. | Focused iOS parser/validator/persistor/dependency/source-guard tests, focused Mac pairing URL builder tests, and the SACB static guard cover the lifecycle contract. | Closed for pairing lifecycle, re-pair rollback, fail-closed forget, and unauthorized repair proof. SACB-10 owns final closeout and broad verification. | SACB-9 pairing lifecycle checkpoint |
| SACB-10 | pending | Not started. | Not run. | Open: final closeout. | pending |

## Baseline Evidence

| Surface | Finding | Required Row |
|---|---|---|
| `packages/agent/src/transport/engine/socket/wire.rs` | `WireContext` accepts public `authorityScopes` and `runtimeMetadata`. | SACB-3 |
| `packages/agent/src/transport/engine/mod.rs` | `build_engine_transport_request` copies caller context authority scopes and runtime metadata into causal context. | SACB-3 |
| `packages/agent/src/domains/capability/operations/filesystem.rs` | `RUNTIME_METADATA_WORKING_DIRECTORY` controls primitive file and process working directories. | SACB-3/SACB-6 |
| `packages/agent/src/engine/authority/grants/derivation.rs` | Child grant file roots are narrowed with string-prefix checks. | SACB-4 |
| `packages/agent/src/engine/authority/grants/model.rs` | Bootstrap grants are wildcard root grants and must be explicitly inventoried. | SACB-4 |
| `packages/agent/src/domains/capability/operations/mod.rs` | Bootstrap grants, public caller contexts, system state scope, and sessionless trace/log/replay reads must fail closed at primitive execute. | SACB-6 |
| `packages/agent/src/domains/capability/operations/process.rs` | `process_run` must prove `networkPolicy none` from the active authority grant before launching a shell. | SACB-6 |
| `packages/agent/src/engine/runtime/external_workers/validation.rs` | Namespace checks used substring matching; scoped worker token validation also allowed wildcard stream selectors and unbound session-visible workers. | SACB-7 |
| `packages/agent/src/engine/runtime/external_workers/registration.rs` | Worker stream publish accepted caller-provided session/topic/visibility and trigger registration did not prove same-worker target ownership. | SACB-7 |
| `packages/agent/src/transport/runtime/external_workers.rs` | Worker invocation results must remain owned by the per-socket pending invocation map and disconnect paths must drain waiters. | SACB-7 |
| `packages/agent/src/domains/session/event_store/redaction.rs` | Redaction covered common API-key/bearer patterns but did not cover JSON/debug-description OAuth/API-key fields used by iOS/Mac diagnostics payloads. | SACB-8 |
| `packages/agent/src/domains/session/event_store/store/event_store/logs.rs` | Client log ingestion relied on client-side redaction before writing durable `logs` rows. | SACB-8 |
| `packages/mac-app/Sources/Support/Diagnostics/DiagnosticsRedactor.swift` | Mac diagnostics redaction lagged iOS and did not cover camelCase OAuth/API-key fields or Swift-description auth fields. | SACB-8 |
| `packages/ios-app/Sources/Support/Pairing/PairedServerStore.swift` | Paired-server metadata must remain token-free so UserDefaults stores only host/port/label/status metadata. | SACB-8 |
| `packages/ios-app/Sources/Support/Storage/PairedServerTokenStore.swift` | iOS bearer tokens must stay in Keychain and be keyed by paired-server id. | SACB-8 |
| `packages/ios-app/Sources/Support/Pairing/PairingURLParser.swift` | Pairing URLs accepted bare host strings but did not reject URL-shaped, path, query, userinfo, bracketed, or malformed host values before probe/storage. | SACB-9 |
| `packages/ios-app/Sources/Support/Composition/DependencyContainer.swift` | Forgetting a paired server removed metadata before deleting the Keychain token and swallowed token deletion errors. | SACB-9 |
| `packages/mac-app/Sources/Support/Pairing/PairingURLBuilder.swift` | Mac QR URL building accepted any non-empty host and any positive port, broader than the iOS pairing parser should accept. | SACB-9 |

## Verification Log

| Command | Result | Evidence |
|---|---|---|
| `git status --short --branch` | exit 0 | Worktree was clean on `codex/primitive-engine-teardown` before SACB-0 edits. |
| `find /Users/moose/.tron/internal/database -maxdepth 2 -type f` | exit 0 | Runtime databases available for direct inspection when later rows depend on runtime state. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | SACB scaffold target passed: 6 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test primitive_code_cleanup_invariants -- --nocapture` | exit 0 | Existing PCC tracked-file inventory target passed after adding SACB rows: 16 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test hierarchical_rearchitecture_invariants -- --nocapture` | exit 0 | Existing HRA tracked-file ownership target passed after adding SACB rows: 35 tests. |
| `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` | exit 0 | Rust formatting check passed after rustfmt applied to new invariant modules. |
| `git diff --check --cached && git diff --check` | exit 0 | Staged and unstaged whitespace checks passed. |
| `node` marker inventory generator | exit 0 | Generated the SACB inventory from tracked security-marker files after excluding non-security token-accounting/model-catalog surfaces; current coverage is 603 rows. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | SACB-1 inventory coverage target passed after adding marker coverage and boundary-class guards: 8 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml app::bootstrap::server --lib -- --nocapture` | exit 0 | SACB-2 focused server route/auth/loopback verification passed: 24 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | SACB-2 static route/auth guard verification passed: 9 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml transport::engine --lib -- --nocapture` | exit 0 | SACB-3 focused engine transport context verification passed: 16 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | SACB-3 static public context trust guard verification passed: 10 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml engine::tests::authority --lib -- --nocapture` | exit 0 | SACB-4 focused authority grant derivation/authorization/bootstrap verification passed: 9 tests, including prefix-sibling and unresolved parent-component path escape regressions. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | SACB-4 static authority grant guard verification passed: 11 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test primitive_code_cleanup_invariants primitive_code_cleanup_inventory_covers_tracked_files -- --nocapture` | exit 0 | SACB-4 PCC inventory verification for new helper passed: 1 test. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants tracked_source_inventory_is_formalized -- --nocapture` | exit 0 | SACB-4 TPC retention inventory verification passed after adding campaign artifact rows discovered by the guard: 1 test. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test hierarchical_rearchitecture_invariants tracked_files_have_rearchitecture_inventory_rows -- --nocapture` | exit 0 | SACB-4 HRA inventory verification for new helper passed: 1 test. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test true_modularity_boundary_invariants boundary_inventory_covers_tracked_sources -- --nocapture` | exit 0 | SACB-4 TMB inventory verification for new helper passed: 1 test. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test state_ownership_lifecycle_invariants sol_inventory_rows_are_structured_and_classified -- --nocapture` | exit 0 | SACB-4 SOL inventory structure verification passed: 1 test. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test state_ownership_lifecycle_invariants sol_inventory_covers_stateful_marker_sources -- --nocapture` | exit 0 | SACB-4 SOL stateful marker inventory verification passed after adding the missing capability copy button row: 1 test. |
| `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` | exit 0 | SACB-4 formatting check passed. |
| `git diff --check` | exit 0 | SACB-4 whitespace check passed. |
| `cargo test --manifest-path packages/agent/Cargo.toml engine::kernel::policy --lib -- --nocapture` | exit 0 | SACB-5 internal visibility policy verification passed: 2 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml domains::agent::prompt::prompt::tests::hidden_prompt_child_context_is_engine_owned_not_public_caller --lib -- --nocapture` | exit 0 | SACB-5 hidden agent prompt delegation causality verification passed: 1 test. |
| `cargo test --manifest-path packages/agent/Cargo.toml engine::tests::invocation::meta_primitives --lib -- --nocapture` | exit 0 | SACB-5 direct `engine::invoke` visibility verification passed: 12 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml transport::engine --lib -- --nocapture` | exit 0 | SACB-5 public transport verification passed: 17 tests, including the guard that public `engine::invoke` never mints `engine.internal.invoke`. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | SACB-5 static guard verification passed: 12 tests. |
| `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` | exit 0 | SACB-5 formatting check passed after applying rustfmt. |
| `git diff --check` | exit 0 | SACB-5 whitespace check passed. |
| `cargo test --manifest-path packages/agent/Cargo.toml domains::capability --lib -- --nocapture` | exit 0 | SACB-6 focused capability-domain verification passed: 3 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test primitive_trace_execution -- --nocapture` | exit 0 | SACB-6 primitive execute trace/regression verification passed: 8 tests, including public-client, bootstrap-grant, system-state, and process network-policy denials. |
| `cargo test --manifest-path packages/agent/Cargo.toml capability_invocation_executor --lib -- --nocapture` | exit 0 | SACB-6 agent capability executor verification passed: 10 tests, including per-call child grant derivation and canonical working-directory propagation. |
| `cargo test --manifest-path packages/agent/Cargo.toml domains::registration::tests::primitive_execute_observes_without_registry_routing --lib -- --nocapture` | exit 0 | SACB-6 registration smoke passed with a derived least-privilege grant and trusted working-directory runtime metadata. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | SACB-6 static guard verification passed with per-call grant, trusted root, process sandbox, state/trace/log/replay guard checks. |
| `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` | exit 0 | SACB-6 formatting check passed. |
| `git diff --check` | exit 0 | SACB-6 whitespace check passed. |
| `cargo test --manifest-path packages/agent/Cargo.toml engine::tests::runtime::external_worker --lib -- --nocapture` | exit 0 | SACB-7 external worker runtime/protocol/soak verification passed: 21 tests, including scoped token binding, wildcard selector, namespace substring, trigger ownership, stream session, and stream selector denials. |
| `cargo test --manifest-path packages/agent/Cargo.toml engine::tests::runtime::restart_chaos --lib -- --nocapture` | exit 0 | SACB-7 durable external worker restart hydration verification passed: 1 test. |
| `cargo test --manifest-path packages/agent/Cargo.toml engine::tests::runtime::triggers --lib -- --nocapture` | exit 0 | SACB-7 trigger runtime ownership and delivery policy verification passed: 11 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml transport::runtime::external_workers --lib -- --nocapture` | exit 0 | SACB-7 socket result ownership and pending-invocation cleanup verification passed: 2 tests. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | SACB-7 static guard verification passed with scoped token, namespace, trigger, stream, and result-ownership checks. |
| `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` | exit 0 | SACB-7 formatting check passed. |
| `git diff --check` | exit 0 | SACB-7 whitespace check passed. |
| `cargo test --manifest-path packages/agent/Cargo.toml domains::session::event_store::redaction --lib -- --nocapture` | exit 0 | SACB-8 server redaction verification passed: 18 tests, including JSON auth fields, debug-description auth fields, and unquoted secret key-value forms. |
| `cargo test --manifest-path packages/agent/Cargo.toml domains::session::event_store::store::event_store::logs --lib -- --nocapture` | exit 101, then exit 0 | Initial fixture placed the redacted marker across the truncation boundary; after correcting the fixture to separate the secret and padding, log-ingest verification passed: 12 tests, including server-side redaction before durable log storage and before truncation. |
| `cargo test --manifest-path packages/agent/Cargo.toml app::lifecycle::onboarding --lib -- --nocapture` | exit 0 | SACB-8 bearer-token lifecycle verification passed: 22 tests, including malformed-file refusal, empty auth sentinel materialization, atomic concurrent writes, and `0o600` mode. |
| `cargo test --manifest-path packages/agent/Cargo.toml domains::auth::credentials::storage --lib -- --nocapture` | exit 0 | SACB-8 provider credential storage verification passed: 77 tests, including malformed-file preservation, atomic writes, temp cleanup, and permission narrowing to `0o600`. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 101, then exit 0 | Initial SACB-8 static guard run found a Keychain wording mismatch and a missing inventory row for `event_store/store/event_store/logs.rs`; after fixing both, the SACB invariant target passed: 15 tests. |
| `cd packages/ios-app && xcodegen generate` | exit 0 | iOS project regenerated after source-guard/doc changes. |
| `cd packages/ios-app && xcodebuild test -scheme Tron -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:TronMobileTests/SourceGuardTests -only-testing:TronMobileTests/DiagnosticsRedactorTests -only-testing:TronMobileTests/PairedServerTokenStoreTests -only-testing:TronMobileTests/PairingPersistorTests` | exit 65, then exit 0 | Initial run exposed a stale exact phrase assertion in `packages/ios-app/docs/architecture.md`; after restoring the canonical `self-feeding diagnostics loop` wording, the focused iOS verification passed: 66 tests across source guards, diagnostics redaction, Keychain token storage, and pairing persistence. |
| `cd packages/mac-app && xcodegen generate` | exit 0 | Mac project regenerated after diagnostics redactor/source-guard changes. |
| `cd packages/mac-app && xcodebuild test -scheme TronMac -destination 'platform=macOS' -only-testing:TronMacTests/DiagnosticsRedactorTests -only-testing:TronMacTests/MacSourceGuardTests` | interrupted | Broad Mac source-guard attempt passed the Mac diagnostics redactor suite but then stalled in the unrelated broad source-guard app-host run; it was interrupted and is not counted as passing evidence. |
| `cd packages/mac-app && xcodebuild test -scheme TronMac -destination 'platform=macOS' -only-testing:TronMacTests/DiagnosticsRedactorTests -only-testing:TronMacTests/MacSourceGuardTests/diagnosticsRedactorKeepsAuthFieldParity` | exit 0 | Focused Mac verification passed the DiagnosticsRedactor suite: 7 tests, including camelCase auth JSON and Swift-description auth fields. Xcode's Swift Testing method selector did not execute an additional source-guard test, so source-marker parity is covered by the passing Rust SACB static guard. |
| `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` | exit 0 | SACB-8 formatting check passed after Rust test fixture and static-guard edits. |
| `git diff --check` | exit 0 | SACB-8 whitespace check passed after code and documentation edits. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | Final post-evidence SACB-8 invariant rerun passed: 15 tests. |
| `cargo fmt --manifest-path packages/agent/Cargo.toml --all` | exit 0 | Rust formatting applied after adding the SACB-9 static guard. |
| `cd packages/ios-app && xcodegen generate` | exit 0 | iOS project regenerated after pairing source/test changes. |
| `cd packages/mac-app && xcodegen generate` | exit 0 | Mac project regenerated after pairing source/test changes. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | SACB-9 static guard passed: 16 tests, including the new iOS/Mac pairing lifecycle boundary guard. |
| `cd packages/ios-app && xcodebuild test -scheme Tron -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:TronMobileTests/PairingURLParserTests -only-testing:TronMobileTests/PairingValidationTests -only-testing:TronMobileTests/PairingPersistorTests -only-testing:TronMobileTests/DependencyContainerTests -only-testing:TronMobileTests/OnboardingStateTests -only-testing:TronMobileTests/PairedServerTokenStoreTests -only-testing:TronMobileTests/WebSocketAuthTests -only-testing:TronMobileTests/SourceGuardTests` | exit 0 | SACB-9 focused iOS verification passed: 141 tests across QR/deep-link/manual host validation, rollback planning, Keychain token storage, fail-closed forget, prefilled re-pair state, unauthorized repair state, and source guards. |
| `cd packages/mac-app && xcodebuild test -scheme TronMac -destination 'platform=macOS' -only-testing:TronMacTests/PairingURLBuilderTests -only-testing:TronMacTests/QRCodeGeneratorTests -only-testing:TronMacTests/ServerPingTests -only-testing:TronMacTests/ServerStatusPollerTests -only-testing:TronMacTests/BearerTokenReaderTests` | exit 0 | SACB-9 focused Mac verification passed: 47 tests across strict QR URL building/parsing, QR round-trip, bearer token read permissions, ping unauthorized classification, and status-poller unauthorized state. |
| `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` | exit 0 | SACB-9 Rust formatting check passed. |
| `git diff --check` | exit 0 | SACB-9 whitespace check passed after code and documentation edits. |
| `cd packages/ios-app && xcodebuild test -scheme Tron -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:TronMobileTests/PairingPersistorTests -only-testing:TronMobileTests/SourceGuardTests/testPairingLifecycleBoundaries` | exit 0 | Post-review fallback removal verification passed the PairingPersistor suite: 11 tests. Xcode's Swift Testing method selector did not execute the named source-guard method, so the full source-guard selector was rerun next. |
| `cd packages/ios-app && xcodebuild test -scheme Tron -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:TronMobileTests/SourceGuardTests` | exit 0 | Post-review iOS source guard verification passed: 41 tests, including `pairing lifecycle validates host input and forgets tokens fail-closed`. |
| `cargo test --manifest-path packages/agent/Cargo.toml --test security_authority_capability_boundaries_invariants -- --nocapture` | exit 0 | Final post-evidence SACB-9 invariant rerun passed: 16 tests. |
