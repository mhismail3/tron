# True Primitive Cleanup Evidence Manifest

Created: 2026-06-09

Scorecard: [`true-primitive-cleanup-scorecard.md`](true-primitive-cleanup-scorecard.md)

Current score: **95/100**

Status: **in_progress**

This manifest records command, source-audit, simulator, database, and commit
evidence for the True Primitive Cleanup campaign. Do not award points in the
scorecard without concrete evidence here.

## Baseline

- Branch: `codex/primitive-engine-teardown`.
- Baseline commit before TPC edits: `d307cdad2`.
- Plan: `/Users/<USER>/Downloads/PLAN (3).md`, redacted from the operator
  Downloads path.
- Compatibility assumption: none for deleted primitive-branch internals.

## Row Evidence

| Row | Status | Evidence summary | Commands / artifacts | Residual risk | Checkpoint |
|-----|--------|------------------|----------------------|---------------|------------|
| TPC-0 | passed_after_fix | Formalized the TPC scorecard/evidence pair, README links, static invariant target, and current hard-LOC red baseline. Red proof showed the new invariant target did not exist and current source/test files exceeded the TPC hard budgets. | Red target proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 101, no test target named `true_primitive_cleanup_invariants`. Red Rust LOC scan -> exit 0, 20 files over TPC Rust limits. Red Swift LOC scan -> exit 0, 11 files over TPC Swift limits. Green setup proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 4 passed; `git diff --check` -> exit 0. | TPC-1 still needs a complete tracked source inventory. TPC-2 through TPC-8 must split or delete the recorded over-budget files. | `498abfb24` |
| TPC-1 | passed_after_fix | Added `true-primitive-cleanup-retention-inventory.md` and `true-primitive-cleanup-retention-inventory.tsv`. The TSV now classifies 1,412 tracked and newly introduced files in TPC scope as `primitive`, `implementation`, `support`, `test`, `docs`, or `delete` after the TPC-10 docs/guards/inventories gate was added. Red proof failed on the missing inventory artifact before generation. | Red proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants tracked_source_inventory_is_formalized -- --nocapture` -> exit 101, missing `true-primitive-cleanup-retention-inventory.md`. TPC-1 green proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants tracked_source_inventory_is_formalized -- --nocapture` -> exit 0, 1 passed; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 5 passed; `git diff --check` -> exit 0. TPC-2 inventory regeneration: TSV updated to 1,370 rows. TPC-3 inventory regeneration: TSV updated to 1,376 rows. TPC-4 inventory regeneration: TSV updated to 1,382 rows. TPC-5 inventory regeneration: TSV updated to 1,389 rows. TPC-6 inventory regeneration: TSV updated to 1,393 rows. TPC-7 inventory regeneration: TSV updated to 1,397 rows. TPC-8 inventory regeneration: TSV updated to 1,405 rows. TPC-9 inventory regeneration: TSV updated to 1,411 rows. TPC-10 inventory regeneration: TSV updated to 1,412 rows. | Final full verification remains owned by TPC-11. | `92521b511` |
| TPC-2 | passed_after_fix | Split the oversized catalog registry, catalog invocation idempotency, ledger, queue, and stream files into concern-owned modules. Removed default no-op durable-worker/function methods from `EngineLedgerStore`; in-memory and test ledgers now implement those methods explicitly. The TPC-2 static gate now proves the original oversized roots are under budget, the split files exist, and no ledger trait durable-catalog default remains. | Red proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants engine_catalog_and_durability_roots_are_split_and_explicit -- --nocapture` -> exit 101, `engine/catalog/registry/mod.rs` had 895 LOC. Green proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo check --manifest-path packages/agent/Cargo.toml --bin tron` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants engine_catalog_and_durability_roots_are_split_and_explicit -- --nocapture` -> exit 0, 1 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib catalog_discovery -- --nocapture` -> exit 0, 16 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib ledger_idempotency -- --nocapture` -> exit 0, 11 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib queue_lifecycle -- --nocapture` -> exit 0, 5 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib queue_inspection_persistence -- --nocapture` -> exit 0, 2 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib streams -- --nocapture` -> exit 0, 16 passed; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 6 passed; `git diff --check` -> exit 0. | Broad residue review closed by TPC-10. | `739612887` |
| TPC-3 | passed_after_fix | Split invocation host construction/bootstrap into `engine/invocation/host/bootstrap.rs` and meta/delegated invocation into `engine/invocation/host/meta_invocation.rs`; split primitive backend stores into `engine/primitives/stores.rs` and worker/function registration assembly into `engine/primitives/workers.rs`; moved trigger runtime helper handlers into `engine/tests/runtime/trigger_helpers.rs`. Added a static TPC-3 gate that proves the original host, primitive, and trigger roots are under their hard limits, the split owners exist, the host root has only the existing catalog-watch re-export, and the primitive root no longer contains `OnceLock` or weak-host store wiring. | Red proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants invocation_host_and_primitive_store_roots_are_narrow -- --nocapture` -> exit 101, `engine/invocation/host/mod.rs` had 880 LOC. Green proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo check --manifest-path packages/agent/Cargo.toml --bin tron` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants invocation_host_and_primitive_store_roots_are_narrow -- --nocapture` -> exit 0, 1 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib host_invocation -- --nocapture` -> exit 0, 14 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib meta_primitives -- --nocapture` -> exit 0, 10 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib triggers -- --nocapture` -> exit 0, 15 passed; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 7 passed; `git diff --check` -> exit 0. | Broad residue review closed by TPC-10. | `c7d16e4b9` |
| TPC-4 | passed_after_fix | Retained external-worker runtime as a loopback-only primitive surface with explicit proof instead of deleting it: lifecycle/heartbeat/disconnect and durable worker health transitions moved to `external_workers/lifecycle.rs`, registration/proxy/stream publication moved to `external_workers/registration.rs`, scoped-token and capability metadata validation moved to `external_workers/validation.rs`, and the over-budget behavior test was split into protocol and helper modules. The TPC-4 gate proves the original runtime/test roots are under budget, the split owners exist, loopback-only policy remains, validation is no longer root-owned, and durable disconnect behavior remains explicit. | Red proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants external_worker_runtime_is_loopback_split_and_proven -- --nocapture` -> exit 101, `engine/runtime/external_workers/mod.rs` had 855 LOC. Green proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo check --manifest-path packages/agent/Cargo.toml --bin tron` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants external_worker_runtime_is_loopback_split_and_proven -- --nocapture` -> exit 0, 1 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib external_worker -- --nocapture` -> exit 0, 16 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib external_worker_soak -- --nocapture` -> exit 0, 1 passed; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 8 passed; `git diff --check` -> exit 0. | Broad residue review closed by TPC-10. | `6860022df` |
| TPC-5 | passed_after_fix | Split provider/auth/model ownership without behavior changes: provider factory, OpenAI message converter, auth credential types, and Ollama stream handler now keep tests in child modules; OpenAI provider request-shaping tests live under `provider/tests/request.rs`; Gemini model metadata lives in `google/types/models.rs`; provider root docs no longer describe exports as compatibility aliases; and the TPC-5 gate proves line budgets plus alias-boundary discipline. | Red proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants provider_auth_model_roots_are_split_and_provider_native -- --nocapture` -> exit 101, `providers/factory.rs` had 888 LOC. Green proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo check --manifest-path packages/agent/Cargo.toml --bin tron` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants provider_auth_model_roots_are_split_and_provider_native -- --nocapture` -> exit 0, 1 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib factory -- --nocapture` -> exit 0, 41 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib message_converter -- --nocapture` -> exit 0, 115 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib credentials::types -- --nocapture` -> exit 0, 30 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib openai::provider -- --nocapture` -> exit 0, 65 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib ollama::stream_handler -- --nocapture` -> exit 0, 19 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib google::types -- --nocapture` -> exit 0, 35 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib pricing -- --nocapture` -> exit 0, 35 passed; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 9 passed; `git diff --check` -> exit 0. | Broad residue review closed by TPC-10. | `449616f2e` |
| TPC-6 | passed_after_fix | Split transport, agent-loop persistence, and observability roots without changing behavior. The socket root now owns session lifecycle and request dispatch while `socket/subscriptions.rs` owns subscription state, stateless poll, ack, push polling, filter narrowing, and cursor advancement. Turn-runner persistence and SQLite observability transport tests now live in child test modules. The moved no-persister persistence test now describes the allowed caller contract without no-op wording. | Red proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants transport_agent_observability_roots_are_split_and_explicit -- --nocapture` -> exit 101, `transport/engine/socket/mod.rs` had 873 LOC. Green proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo check --manifest-path packages/agent/Cargo.toml --bin tron` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants transport_agent_observability_roots_are_split_and_explicit -- --nocapture` -> exit 0, 1 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib transport::engine::socket -- --nocapture` -> exit 0, 9 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib turn_runner::persistence -- --nocapture` -> exit 0, 9 passed; `cargo test --manifest-path packages/agent/Cargo.toml --lib observability::transport -- --nocapture` -> exit 0, 12 passed; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 10 passed; touched TPC-6 residue scan for `noop|no-op|fallback|legacy|compat|deprecated` -> exit 1, no matches; current Rust hard-budget scan -> exit 0, only `packages/agent/src/app/bootstrap/tests.rs` remains over budget; `git diff --check` -> exit 0. | Broad residue review closed by TPC-10. | `5b4d43641` |
| TPC-7 | passed_after_fix | Split iOS setup, diagnostics, and generated-runtime roots into concern-owned files. `SetupSteps.swift` now owns the step flow while `SetupStepComponents.swift` owns reusable setup controls. `DiagnosticsBundleBuilder.swift` now owns assembly while `DiagnosticsBundleTypes.swift` owns bundle DTOs, sanitization, hashing, and host classification. `GeneratedRuntimeSurfaceView.swift` now owns rendering flow and state while `GeneratedRuntimeSurfaceView+RenderingHelpers.swift` owns pure presentation/formatting helpers. The TPC-7 static gate also preserves the temporary event cache as explicit server-authoritative projection state rather than fallback ownership. | Red proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants ios_engine_protocol_roots_are_split_and_cache_mode_explicit -- --nocapture` -> exit 101, `SetupSteps.swift` had 624 LOC. Green proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants ios_engine_protocol_roots_are_split_and_cache_mode_explicit -- --nocapture` -> exit 0, 1 passed; `cd packages/ios-app && xcodegen generate` -> exit 0; `xcodebuild test -scheme Tron -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:TronMobileTests/SourceGuardTests` -> exit 0, 38 Swift Testing tests passed; `xcodebuild test -scheme Tron -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:TronMobileTests/DiagnosticsBundleBuilderTests -only-testing:TronMobileTests/GeneratedUIRendererTests -only-testing:TronMobileTests/OnboardingStateTests` -> exit 0, 34 Swift Testing tests passed. An earlier concurrent functional-suite run failed with Xcode build database lock while SourceGuard was building; it was rerun serially and passed. Full TPC suite `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 11 passed; source-only touched-root residue scan for `fallback\|legacy\|compat\|deprecated\|noop\|no-op` -> exit 1/no matches; `git diff --check` -> exit 0. | Broad docs/script residue review closed by TPC-10. | `acaa247ee` |
| TPC-8 | passed_after_fix | Split iOS UI/session state roots without behavior changes. `SettingsView.swift` now owns shell orchestration while `SettingsView+MainSection.swift` owns the main grid and action sections. `SettingsSupport.swift` now keeps shared row primitives while `SettingsServerSupport.swift` owns paired-server rows and menus. `ChatView.swift` now owns the shell while `ChatView+MessageList.swift` owns message-list and pagination rendering. `ChatViewModel.swift` now keeps state/orchestration while `ChatViewModel+RuntimeCallbacks.swift` owns streaming, UI queue, capability completion, and event callback wiring. `ModelPickerSheet.swift` now owns the sheet frame while `ModelPickerSheet+Sections.swift` owns provider/family/model/reasoning sections. `TronColors.swift` now owns base colors while `TronThemeTokens.swift` owns derived tokens. Typewriter animation coverage moved into `StreamingManagerTypewriterTests.swift`. | Red proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants ios_ui_state_roots_are_split_and_under_budget -- --nocapture` -> exit 101, `SettingsView.swift` had 698 LOC. Green proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants ios_ui_state_roots_are_split_and_under_budget -- --nocapture` -> exit 0, 1 passed; `cd packages/ios-app && xcodegen generate` -> exit 0; `xcodebuild test -scheme Tron -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:TronMobileTests/SourceGuardTests` -> exit 0, 38 Swift Testing tests passed after updating the intentional messaging test-owner count and near-budget watch rows; `xcodebuild test -scheme Tron -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:TronMobileTests/StreamingManagerTests -only-testing:TronMobileTests/StreamingManagerTypewriterTests -only-testing:TronMobileTests/ChatViewModelEventRoutingTests -only-testing:TronMobileTests/ChatViewModelLifecycleTests -only-testing:TronMobileTests/SheetCoordinatorTests -only-testing:TronMobileTests/TronColorsTests -only-testing:TronMobileTests/ChipStyleModifierTests -only-testing:TronMobileTests/AppearanceSettingsTests -only-testing:TronMobileTests/ThinkingIndicatorStyleTests -only-testing:TronMobileTests/AgentSettingsPageLayoutTests -only-testing:TronMobileTests/SettingsPageContainerTests -only-testing:TronMobileTests/ModelPickerStateTests` -> exit 0, 147 selected tests passed across XCTest and Swift Testing; full TPC suite `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 12 passed; source-only touched-root residue scan for `fallback\|legacy\|compat\|deprecated\|noop\|no-op` -> exit 1/no matches; current Swift hard-budget scan -> exit 0/no output; current Rust hard-budget scan -> exit 0, only `packages/agent/src/app/bootstrap/tests.rs` remains over budget; `git diff --check` -> exit 0. | No TPC-8 Swift LOC or moved-concern blocker remains; broad docs/script residue review closed by TPC-10. | `10e6aa8ba` |
| TPC-9 | passed_after_fix | Split `app/bootstrap/tests.rs` into `tests/cli.rs`, `tests/database.rs`, `tests/provider_auth.rs`, `tests/server_runtime.rs`, and `tests/source_guards.rs`. Renamed the contributor deploy module to `scripts/tron.d/manual-deploy.sh` and the command to `tron manual-deploy`; no old `tron deploy` alias remains. Updated workspace and installed CLI help, service recovery guidance, README CLI/deploy docs, and removed inactive-operation wording from Mac runtime helper comments. | Red proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants script_runtime_helpers_are_split_and_manual_only -- --nocapture` -> exit 101, `app/bootstrap/tests.rs` had 832 LOC. Green proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --lib app::bootstrap::tests -- --nocapture` -> exit 0, 45 passed; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 13 passed; `bash -n scripts/tron scripts/tron-cli scripts/tron.d/manual-deploy.sh scripts/tron.d/workspace.sh scripts/tron-lib.d/service.sh` -> exit 0; `scripts/tron help | rg -n "manual-deploy\|deploy"` -> exit 0 with only preflight/manual-deploy lines; `scripts/tron deploy` -> exit 1 unknown command before any deploy workflow ran; Mac/scripts source scan for `no-op\|noop` -> exit 1/no matches; current Rust hard-budget scan -> exit 0/no output; current Swift hard-budget scan -> exit 0/no output; `git diff --check` -> exit 0. | No TPC-9 LOC, deploy-command, or Mac/script inactive-operation blocker remains; broad residue/docs cleanup closed by TPC-10. | `bc9d1950c` |
| TPC-10 | passed_after_fix | Added the final docs/guards/inventories static gate, updated active README wording, refreshed HRA/PCC ownership inventories for `scripts/tron.d/manual-deploy.sh`, removed old deploy-command spelling from active reference docs, regenerated the TPC retention inventory for the new guard, and recorded the residual-term review policy in the scorecard. | Red proof: `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants docs_guards_and_inventories_are_current -- --nocapture` -> exit 101, scorecard was still 90/100. Green proof: `cargo fmt --manifest-path packages/agent/Cargo.toml --all -- --check` -> exit 0; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants docs_guards_and_inventories_are_current -- --nocapture` -> exit 0, 1 passed; `cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture` -> exit 0, 14 passed; `cargo test --manifest-path packages/agent/Cargo.toml --test hierarchical_rearchitecture_invariants live_docs_scripts_and_workflows_do_not_claim_old_paths -- --nocapture` -> exit 0, 1 passed; scoped active-reference stale deploy/residue scan -> exit 1/no matches; current Rust hard-budget scan -> exit 0/no output; current Swift hard-budget scan -> exit 0/no output; inventory count check -> `primitive 105`, `implementation 478`, `support 380`, `test 417`, `docs 32`; `git diff --check` -> exit 0. | Final full verification remains TPC-11. | pending |
| TPC-11 | pending | Pending. | Pending. | Pending. | pending |

## Red Baseline Commands

```bash
cargo test --manifest-path packages/agent/Cargo.toml --test true_primitive_cleanup_invariants -- --nocapture
```

Exit code: 101. The target did not exist before TPC-0.

```bash
find packages/agent/src packages/agent/tests -type f -name '*.rs' -exec sh -c 'for f; do n=$(wc -l < "$f" | tr -d " "); case "$f" in *tests*|*/tests.rs) limit=800;; *) limit=750;; esac; if [ "$n" -gt "$limit" ]; then printf "%s\t%s\tlimit=%s\n" "$n" "$f" "$limit"; fi; done' sh {} + | sort -nr
```

Exit code: 0. Output recorded in the scorecard Rust over-budget baseline.

```bash
find packages/ios-app/Sources packages/ios-app/Tests packages/mac-app/Sources packages/mac-app/Tests -type f -name '*.swift' -exec sh -c 'for f; do n=$(wc -l < "$f" | tr -d " "); case "$f" in *Tests*) limit=650;; *) limit=575;; esac; if [ "$n" -gt "$limit" ]; then printf "%s\t%s\tlimit=%s\n" "$n" "$f" "$limit"; fi; done' sh {} + | sort -nr
```

Exit code: 0. Output recorded in the scorecard Swift over-budget baseline.
