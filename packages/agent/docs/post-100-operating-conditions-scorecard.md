# Post-100 Operating Conditions And UI/UX Regression Scorecard

Status: active operating scorecard

Created: 2026-05-30

Initial score: **0/100**

Current score: **76/100** (raw **125/165**; ROC-5 resource truth and mutation failure paths passed after fixing duplicate process materialization validation and live harness determinism; UXR-1 iPhone checkpoint recorded but unscored until remaining action coverage is closed; iPad-specific coverage is deferred to a later plan/session by user request on 2026-05-30)

Completed prerequisite scorecards:
- `collapsed-engine-hardening-scorecard.md`: **100/100**
- `codebase-cleanup-scorecard.md`: **100/100**

No scored collapsed-engine hardening scenario remains open after RWO-N17; this
scorecard owns post-100 regression, breadth, and UI/UX evidence from here.

This scorecard measures real operating confidence after the collapsed engine and
cleanup campaigns reached 100/100. It covers production install behavior, live
server and database truth, provider breadth, long-running churn, and UI/UX
projection correctness in the iOS simulator and Mac wrapper.

Raw scenario weights sum to 165. The displayed score is normalized to 100 and
only reaches **100/100** when every ROC and UXR row is passed or explicitly
closed with tested, documented non-applicability.

## Operating Loop

Every scenario follows the same loop:

1. Read this scorecard and pick the next incomplete scenario.
2. Run the exact app/server path under real conditions.
3. Inspect DB truth: logs, events, invocations, resources, approvals, queues,
   streams, sessions, worktrees, and notification rows as relevant.
4. Inspect visible UI with simulator screenshots and Computer Use whenever a
   button, icon, sheet, menu, or visible state matters.
5. Classify the result as `passed`, `passed_after_fix`, `blocked`, or
   `failed_unfixed`.
6. If failed, stop breadth testing, isolate the owner, add the smallest focused
   test, fix the root cause, remove nearby dead/fallback/legacy/compatibility
   code, rerun the exact scenario, update this scorecard and ledger, and commit
   the checkpoint.

UI failures use one owner: `ios_rendering`, `ios_state_projection`,
`ios_action_wiring`, `server_contract`, `stream_or_reconstruction`,
`mac_wrapper_ui`, or `test_harness`.

## Evidence Contract

Each UI/UX row must record the simulator UDID, app bundle id, server PID,
session ids, screenshot paths, Computer Use confirmation, exact UI action
sequence, DB invocation/event/resource/approval/queue/log summary after the
scenario start, focused tests added or rerun, owner classification for every
failure, score delta, and residual risk.

Minimum checkpoint commands:

```bash
git status --short
cargo test --manifest-path packages/agent/Cargo.toml --test threat_model_invariants -- --nocapture
git diff --check
```

iOS checkpoints also run:

```bash
cd packages/ios-app
xcodegen generate
xcodebuild test -scheme Tron -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:<targeted-test>
```

App-path evidence starts from:

```bash
scripts/tron dev -bd --json --wait 30
curl -fsS http://localhost:9847/health
xcrun simctl launch <udid> com.tron.mobile.beta
xcrun simctl io <udid> screenshot /tmp/<scenario>.png
```

Do not deep-link newly created harness sessions into the visible Simulator by
default. Use Computer Use to tap through the real iPhone workflow for ordinary
UI evidence. Reserve `xcrun simctl openurl ... tron://session/<session_id>` for
scenarios whose subject is deep-link routing, or for explicitly called-out
manual evidence where the session jump itself is intentional.

## ROC Scenario Ledger

| ID | Scenario | Raw Points | Status | Evidence | Open Loops |
|---|---|---:|---|---|---|
| ROC-0 | Scorecard formalization and baseline | 5 | passed_after_fix | 2026-05-30 baseline: new scorecard, README/iOS/Mac doc links, and static gate added. First `scripts/tron ci fmt check clippy test` stopped at formatting in the new invariant; `cargo fmt --manifest-path packages/agent/Cargo.toml --all` fixed it, then full CI passed. Focused iOS smoke passed on iPhone 17 Pro simulator with `ContentViewCoordinatorTests` and `PendingSessionDeepLinkTests` (7 tests). Dev server `scripts/tron dev -bd --json --wait 30` passed with PID 22061 and `/health` returned ok. Simulator UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`, Computer Use confirmed the visible Sessions list, screenshot `/tmp/roc0-baseline-simulator.png`. DB classification since `2026-05-30T18:06:09Z`: 0 error/fatal logs, 0 failed invocations; warning-only rows were simulator WebSocket attempts to an older paired host. | None for ROC-0; continue with UXR-1 and UXR-2. |
| ROC-1 | Production install, service, and update path | 10 | passed_after_fix | 2026-05-31 checkpoint passed after fixes for owners `mac_wrapper_ui`, `ios_state_projection`, and `ios_action_wiring`: stale `/Applications/Tron.app` beta6 helper first reproduced the correct no-optimistic-success path by loading but failing `/health`; local Release build beta7 was copied into `/Applications/Tron.app`; `LiveLaunchAgentManager` now treats stale same-bundle parent builds and `needs LWCR update` launch constraints as registered-not-ready so the installed app unregisters/re-registers through SMAppService before launchd runs the helper; `scripts/tron start`/`restart` prefer the installed wrapper command path and report success only after `/health`; command-mode start now records `internal/run/mac-app-version.json` only after the health gate. No optimistic success before `/health`: every start-like path remained visibly failed until the installed helper answered healthy. Live evidence: `codesign --verify --deep --strict` passed for the built and installed Release app; `/Applications/Tron.app/Contents/MacOS/Tron --tron-start-server-and-quit` exited 0; `scripts/tron status --json` reported prod healthy with listener PID `77197`; `curl http://localhost:9847/health` returned `status=ok`; `launchctl print gui/$(id -u)/com.tron.server` showed `state = running`, parent bundle `com.tron.mac`, parent bundle version `7`, no `needs LWCR update`, and last exit code `0`; `~/.tron/internal/run/mac-app-version.json` recorded `canonicalVersion=0.1.0-beta.7`, `buildNumber=7`. Stop/start/restart were exercised live, and command-mode uninstall unregistered the service while preserving `~/.tron/internal/database/tron.sqlite` and `~/.tron/workspace`; subsequent `scripts/tron start` restored a healthy production helper. The iPhone-only app-path check also caught and fixed a bad Source Control UX for a newly created Quick Chat session without an isolated worktree: passthrough repo status (`hasWorktree=true`, `worktree.isolated=false`) now remains branch context only, suppresses dashboard dirty/worktree icons, omits the Agent Control Source Control card, avoids diff/repo metadata calls, and no longer presents `This session no longer has a worktree` without user-initiated mutation; Computer Use first reproduced the bad visible `No Worktree` card, then verified the fixed Agent Control sheet on iPhone 17 Pro simulator UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`, shows Context, Model, Analytics, History, and Session ID with no Source Control branch glyph/card, screenshot `/tmp/roc1-agent-control-no-source-control-202605310300.png`. Verification passed: full Mac `xcodebuild test -scheme TronMac -destination 'platform=macOS'` (291 tests, xcresult `Test-TronMac-2026.05.30_19-46-48--0700`), focused Mac command/startup/health/launch-agent tests (29 tests), Release `xcodebuild` build, Rust `threat_model_invariants` (65 tests), focused iOS source-control/worktree/session-icon tests including `SourceControlCardStateTests`, `WorktreeInfoTests`, `SessionTitleIconsTests`, `WorktreeStatusCacheTests`, and `WorktreeIsolationStateTests` after both no-sheet and no-card fixes, `cargo fmt --all -- --check`, and `git diff --check`. | None for ROC-1. Continue with ROC-2 hosted model regression matrix. |
| ROC-2 | Current hosted model regression matrix | 8 | passed_after_fix | 2026-05-31 checkpoint passed after fixing owner `test_harness`: added reusable live fixture `packages/agent/tests/fixtures/roc2_hosted_model_matrix.py`, which connects to `/engine` over WebSocket without printing credentials, queries `model::list`, selects the current recommended Gemini model (`gemini-3.1-pro-preview`), creates one disposable session per hosted model, prompts each model to use only `capability::execute`, and validates DB truth for parent/child invocations, resources, approvals, compact events, and logs. The first harness run (`/tmp/roc2_hosted_model_matrix_20260530200644.json`) correctly failed because the harness prompt omitted required `evidence.payload.summary`; `resource::create` rejected the malformed payload with a schema violation and no evidence resource was created. After adding the required summary field, the final run `/tmp/roc2_hosted_model_matrix_20260530200808.json` passed for `claude-sonnet-4-6`, `gpt-5.5`, and `gemini-3.1-pro-preview`. Sessions: Claude `sess_019e7c00-d15b-73b3-9710-849dc00e6398`, GPT `sess_019e7c01-0497-7333-842e-4875e438dd74`, Gemini `sess_019e7c01-3010-7543-8c41-152143da75a0`. Each session recorded exactly two successful `capability::execute` calls with child functions `filesystem::read_file` and `resource::create`, the README read returned lines 1-3, and the evidence resources were `evidence:roc2:claude-sonnet-4-6:20260530200808-1`, `evidence:roc2:gpt-5.5:20260530200808-2`, and `evidence:roc2:gemini-3.1-pro-preview:20260530200808-3`; each had a resource version row. DB validation for the final run: failed invocations `0`, pending approvals `0`, total approvals `0`, error/fatal logs `0`, compact events `0`, model/provider events showed `anthropic`, `openai`, and `google` respectively, and `/health` was ok before and after. iPhone-only Simulator evidence on iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`: Computer Use confirmed the Gemini and Claude chat screens show the prompt, execute capability cards, terminal marker, and model chip; screenshots `/tmp/roc2-gemini-iphone-202605310309.png`, `/tmp/roc2-gpt-iphone-202605310309.png`, and `/tmp/roc2-claude-iphone-202605310309.png`. Verification passed: `python3 -m py_compile packages/agent/tests/fixtures/roc2_hosted_model_matrix.py` and the final live harness run. | None for ROC-2. Continue with ROC-3 local model breadth. |
| ROC-3 | Local model breadth | 6 | passed | 2026-05-31 checkpoint passed for the installed local model lane after adding `packages/agent/tests/fixtures/roc3_local_model_breadth.py`, a live `/engine` WebSocket harness that reuses the hosted matrix helpers, creates local-profile sessions with `useWorktree=false`, records `ollama list`, queries `model::list`, separates substrate failures from model-comprehension misses, and runs an optional one-call `capability::execute` attempt. Local catalog truth: `gemma4:e4b` available with provider `ollama`; `gemma4:26b` present in the catalog but unavailable with reason `Not installed — run: ollama pull gemma4:26b`; `ollama list` showed only `gemma4:e4b` installed. Final run `/tmp/roc3_local_model_breadth_20260530201308.json` passed with no validation problems. Substrate smoke session `sess_019e7c05-61f9-7761-add1-f8fbd06dd43b` returned marker `ROC-3 LOCAL SUBSTRATE gemma4:e4b 20260530201308-1`, recorded providerType `ollama`, made `0` execute calls, and had failed invocations `0`, error/fatal logs `0`, compact events `0`. Capability attempt session `sess_019e7c05-b8b5-7e11-adc3-0c9ffbc3fbd7` was classified `passed`: exactly one `capability::execute` call produced child function `filesystem::read_file`, the final assistant marker said README line 1 was read, and DB checks showed failed invocations `0`, error/fatal logs `0`, compact events `0`. iPhone-only Simulator evidence on iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`: Computer Use confirmed the substrate chat showed the marker and `Gemma 4 E4B` model chip, and the execute chat showed the user prompt, `Read File README.md` capability card, terminal marker, and model chip; screenshots `/tmp/roc3-local-substrate-iphone-202605310314.png` and `/tmp/roc3-local-execute-iphone-202605310314.png`. Verification passed: `python3 -m py_compile packages/agent/tests/fixtures/roc3_local_model_breadth.py`, the final live harness run, `cargo test --manifest-path packages/agent/Cargo.toml --test threat_model_invariants -- --nocapture`, and `git diff --check`. | Larger local lane not run because no larger Ollama model is installed; this is cataloged as unavailable rather than an engine failure. Continue with ROC-4 worker, queue, stream, trigger chaos. |
| ROC-4 | Worker, queue, stream, trigger chaos | 9 | passed_after_fix | 2026-05-31 checkpoint passed after tightening owner `test_harness`: added `packages/agent/tests/fixtures/rwo_n15_live_agent_harness.py` for canonical RWO-N15 agent-path evidence, removed stale `old_simulator` naming from active RWO-N16/N16B/N17 harnesses, and upgraded RWO-N16/N16B/N17 DB checks to fail on error logs, stale leases/open queues/unclosed harness subscriptions, missing unregister cleanup, and cross-session leakage where applicable. Real dev-server setup: initial `scripts/tron dev -bd --json --wait 30` correctly failed because production helper PID `77197` held `~/.tron/internal/database/tron.sqlite`; `scripts/tron stop` stopped the installed service, then `scripts/tron dev -bd --json --wait 30` started dev takeover PID `37136`, mode `dev_takeover`, `/health` ok. iPhone-only simulator UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`. RWO-N15 run `/tmp/rwo_n15_agent_run_20260530202240.json`, session `sess_019e7c0e-1cca-7c70-9a98-9737a70338ed`, screenshot `/tmp/rwo_n15_20260530202240_iphone.png`: queue completed with attempts `0`, enqueue/claim/complete events each `1`, worker event `1`, evidence resource `evidence:rwo-n15-agent:20260530202240`, worker/function/trigger unregistered, failed invocations `0`, approvals `0`, compact events `0`, error/fatal logs `0`, open queues `0`, active harness subscriptions `0`, active resource leases `0`, terminal guard `end_turn_stable`. RWO-N16 run `/tmp/rwo_n16_agent_run_20260530202705.json`, session `sess_019e7c12-2ab6-78b3-a58f-18491b163628`, screenshot `/tmp/rwo_n16_20260530202705_iphone.png`: disconnected delivery produced one `queue.fail`, retry completed the same receipt with attempts `1`, worker retry events `5`, evidence resource `evidence:rwo-n16-agent:20260530202705`, no failed target invocation rows, no approvals, no compact events, no error/fatal logs, no open queues, no active harness subscriptions, no active leases, and unregister cleanup passed. RWO-N16B run `/tmp/rwo_n16b_agent_run_20260530202838.json`, session `sess_019e7c13-965c-7740-9cfe-c46f25d10c34`, screenshot `/tmp/rwo_n16b_20260530202838_iphone.png`: cancellation receipt `019e7c13-e2f0-7013-9280-a60514da6c02` ended `cancelled`, attempts `0`, no lease, `queue.cancel=1`, `queue.complete=0`; dead-letter receipt `019e7c14-219e-7c70-9acd-49dbbeb07aaa` ended `dead_lettered`, attempts `3`, expected target failures `3`, unexpected failed invocations `0`, `queue.dead_letter=1`; approvals `0`, compact events `0`, error/fatal logs `0`, open queues `0`, active harness subscriptions `0`, active leases `0`, terminal guard `end_turn_stable`. RWO-N17 run `/tmp/rwo_n17_multi_session_run_20260530203117.json`, visible session `sess_019e7c16-015b-78d3-9ba6-7dbdcdb3f8a3`, background session `sess_019e7c16-46b3-7d22-a4d5-dd6d92bc29bb`, screenshots `/tmp/rwo_n17_visible_before_20260530203117.png`, `/tmp/rwo_n17_background_20260530203117.png`, and `/tmp/rwo_n17_visible_after_20260530203117.png`: visible and background terminal guards passed, simulator open/screenshot return codes passed, background queue completed attempts `0`, worker/function/trigger unregistered, no failures/approvals/compact events/error logs/open queues/active harness subscriptions/active leases, resources `evidence:rwo-n17-visible:20260530203117` and `evidence:rwo-n17-background:20260530203117` present, and cross-leakage counts were `0`. Computer Use manually confirmed the RWO-N16B terminal iPhone screen reports both cancellation and dead-letter outcomes and the RWO-N17 visible screen shows the create capability card, terminal marker, and `Sonnet 4.6` chip. | None for ROC-4. Continue with ROC-5 resource truth and mutation failure paths. |
| ROC-5 | Resource truth and mutation failure paths | 7 | passed_after_fix | 2026-05-31 checkpoint passed after fixes for owners `server_contract` and `test_harness`: `process::run` now rejects duplicate `sandbox_materialized.expectedOutputs[].path` and duplicate resolved `targetPath` destinations before sandbox spawn or approval, with focused Rust coverage in `sandbox_materialized_duplicate_output_path_is_invalid_before_approval`, `sandbox_materialized_duplicate_target_path_is_invalid_before_approval`, and `sandbox_materialized_duplicate_target_path_rejects_before_spawn`. Added `packages/agent/tests/fixtures/roc5_resource_truth_matrix.py`, a live agent/execute matrix for CAS mismatch, missing payload, declared hash mismatch, discarded resource read, stale generated UI submit, hash verification after out-of-band tampering, and process output collision. The first deterministic run proved the model skipped two negative calls; the harness now repairs missing expected attempts with per-call prompts and waits for a new terminal event after each prompt, avoiding the prior `SESSION_BUSY` race. The DB classifier also counts pre-child `capability::execute` rejections by corrected request idempotency key, because missing payload and process collision intentionally create no target child invocation. Final run `/tmp/roc5_resource_truth_matrix_20260530211203.json`, session `sess_019e7c3b-54e1-72d3-b6d7-fc36b87e3e06`, dev takeover PID `71800`, iPhone 17 Pro simulator UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`: validation passed with `casVersionCount=2`, declared-hash resource absent, damaged resource lifecycle `damaged`, discarded lifecycle `discarded`, stale UI child count `0`, process collision target file absent, all six expected negative attempts present, unexpected failed invocations `0`, approvals `0`, compact events `0`, open queues `0`, active leases `0`, error/fatal logs `0`, terminal guard ok, and `/health` ok. Harness-created sessions no longer deep-link into the visible Simulator by default; Computer Use verified the latest session only after app relaunch refreshed the dashboard and the row was tapped manually, showing the final `Hash Verify` card, `ROC-5 resource truth matrix 20260530211203 complete`, and `Sonnet 4.6` chip; screenshot `/tmp/roc5-resource-truth-iphone-20260530211203-manual.png`. | None for ROC-5. Continue with ROC-6 platform surfaces. |
| ROC-6 | Platform surfaces | 7 | pending | Notifications/APNs, updater, transcription, voice notes, feedback diagnostics, and settings parity. | Failures visible and bounded; no side-channel truth. |
| ROC-7 | Long-running session and compaction regression | 8 | pending | Long sessions, repeated tool calls, restart mid-turn, compaction threshold crossing, background/foreground, and session reopen. | Reconstructed chat equals persisted event truth; no unexpected `compact.*` events. |
| ROC-8 | Large test and static-gate maintainability | 5 | pending | Keep large-file budgets current and split only when it reduces concepts. | Static tests assert owner-level invariants, not brittle old paths. |
| ROC-9 | Full real-world sweep | 5 | pending | Broad Rust CI, focused iOS tests, simulator smoke, DB classification, and large-file audit. | Final sweep remains open until all fixes land. |
| ROC-10 | Closeout | 5 | pending | Close or explicitly defer every row with evidence. | Move future work to a new scorecard, not vague residual risk. |

## UI Inventory

| Surface | Owner | Current Test Coverage | Live/Simulator Coverage | DB Evidence Requirement |
|---|---|---|---|---|
| iOS dashboard/sidebar/session cards | ios_state_projection | `ContentViewCoordinatorTests`, `SessionTitleIconsTests`, `WorktreeStatusCacheTests`, `DashboardCapabilityStreamTests`, `SessionSwitchingTests`, `ForkNavigationTests`, `WorktreeIsolationStateTests`, `LiveSessionPruningTests`, `DatabaseSchemaTests`, and `EventDatabaseTests` cover dashboard selection, metadata projection, processing persistence, branch/fork/dirty rules, and cache refresh. | UXR-1 iPhone screenshots already cover visible card metadata and tap-open; archive/delete still needs action-time-confirmed mutation evidence. iPad/sidebar layout is deferred to a later plan/session. | Session rows, cached session schema, worktree status, branch/fork/dirty state, processing state, archive/delete capability invocations. |
| Chat input and message bubbles | stream_or_reconstruction | `ChatViewModelEventRoutingTests`, `ChatViewModelLifecycleTests`, `ChatViewModelSequenceDedupTests`, `ChatViewModelProcessingIndicatorTests`, `MessagingCoordinatorTests`, `MessageQueueStateTests`, `InputBarStateTests`, `UnifiedEventTransformerTests`, `MessagePayloadTests`, `TextDeltaPluginTests`, `TurnEndPluginTests`, `StreamingManagerTests`, `StreamingRecoveryTests`, `TextStreamConvergenceTests`, and `TurnGroupingTests` cover live/stored prompt and response projection. | UXR-3 iPhone pass covers harness-submitted prompt, native typed prompt, terminal assistant response, live deep link, reconnect by relaunch, and restored session state. | Same session id across `message.user`, assistant terminal events, stream cursors, queue rows, reconstructed history, and no unexpected `compact.*` events. |
| Capability cards and approval sheets | ios_action_wiring | `CapabilityInvocationCoordinatorTests`, `CapabilityInvocationDisplayModelTests`, `CapabilitySharedComponentsTests`, `CapabilityErrorRenderingTests`, `CapabilityInvocationStartedPluginTests`, `CapabilityInvocationCompletedPluginTests`, `CapabilityInvocationGeneratingPluginTests`, `CapabilityInvocationOutputPluginTests`, `CapabilityLifecyclePluginTests`, `ApprovalClientTests`, `EngineApprovalStateTests`, `EngineApprovalTimelineTests`, `UserInteractionCoordinatorTests`, `UserInteractionStateTests`, and `InteractionPolicyTests` cover invocation lifecycle, approval decisions, read-only sheet state, resolving-state gating, and stale action dismissal. | UXR-3 iPhone pass captured completed, error, pending, and denied cards plus pending approval sheet dismissal; UXR-5 iPhone pass covers approve/double-tap, read-only resolved approvals, and process/filesystem/resource/web/model/settings detail sheets. | Capability invocation rows, approval rows/status transitions, grant/resource refs, idempotency keys, and terminal-state action rejection. |
| Generated UI | server_contract | `GeneratedUIDTOTests`, `GeneratedUIRendererTests`, `DisplayClientTests`, `DisplayStreamStateTests`, and Rust `generated_ui` / `resource_kernel` suites cover DTO decoding, native rendering, refresh/action clients, display streams, resource CAS, and stale submit rejection. | UXR-5 iPhone pass covers Engine Console surface render, form submit, refresh, and stale-action rejection. | `ui_surface` resource kind, version, action id, target invocation id, action resource refs, CAS/stale rejection, and generated UI capability invocation rows. |
| Notification bell/list/detail | ios_action_wiring + server_contract | `NotificationClientTests`, `NotificationStoreTests`, `NotificationPillTests`, `NotificationInboxFilterTests`, `PushNotificationServiceTests`, `notification_resources`, and `notifications::tests` cover action idempotency/context, server unread-count responses, resource decision truth, offline failure state, filters, pills, APNs envelope handling, and sheet-content shape. | UXR-2 iPhone manual pass covers bell count/refresh, detail mark-read, deep-link session auto-read, global Read All, and offline failure toast. iPad notification layout is deferred to a later plan/session. | `notifications::mark_read` / `notifications::mark_all_read` invocation rows, notification and decision resource links, session-scoped context, global unread count, and failed-invocation count after seed. |
| New Session and onboarding | ios_state_projection | `NewSessionFlowTests`, `QuickSessionWorkspaceTests`, `ContentViewCoordinatorTests`, `PendingSessionDeepLinkTests`, `OnboardingStateTests`, `PairingValidationTests`, `PairingPersistorTests`, `PairingProbeTests`, `PairingURLParserTests`, `PairedServerStoreTests`, `PairedServerTokenStoreTests`, and `BindingPasteAwareTests` cover sheet modes, quick workspace selection, pairing parse/probe/persist, and unauthorized/unreachable states. | UXR-7 iPhone pass covers pairing deep link, manual-token hydration, bad-token rejection, disconnected retry, and Settings-launched onboarding. UXR-8 still owns session route handoff and broader navigation. | `session::create`/import invocation rows, pairing token/server status truth, workspace refs, route target session id, and connection failure classification. |
| Settings, model/provider settings | server_contract | `SettingsParityTests`, `SettingsStateTests`, `EngineProtocolTypesSettingsTests`, `SettingsClientTests`, `ServerSettingsPageTests`, `AgentContextSettingsPageTests`, `ProvidersSettingsPageTests`, `ProvidersStatusHelpersTests`, `ProviderAssetIconsTests`, `ModelClientTests`, `ModelFilteringServiceTests`, `ModelPickerStateTests`, `AppearanceSettingsTests`, and `FontSettingsTests` cover server-field parity, sparse updates, unreachable handling, providers, model filtering, and appearance controls. | UXR-7 iPhone pass covers loading/unreachable/retry, API key add/edit, OAuth status states, model picker, protected branches, appearance, notifications, diagnostics, and profile TOML truth. | `settings::*` invocation rows, provider credential status/auth resources, profile TOML truth, model catalog response, and failed/unreachable action visibility. |
| Source-control sheets | ios_action_wiring | `SourceChangesSheetTests`, `GitActionRunnerTests`, `GitTileGatingTests`, `WorktreeClientTests`, `WorktreeStatusCacheTests`, `EngineProtocolTypesWorktreeTests`, `EventPluginTests`, `ChatViewModelEventRoutingTests`, `EngineProtocolTypesSettingsTests`, `SettingsStateTests`, `OnboardingStateTests`, `ForkButtonTests`, `GitErrorMessageTests`, `SourceGuardTests`, and `SourceControlCardStateTests` cover sheet state, action wiring, branch/worktree DTOs, protected gating, strict git settings decode, event-origin projection, no-worktree/passthrough gating, and classified git errors. | UXR-6 iPhone pass covers summary card, commit, push, rebase, merge/finalize, pull/fast-forward, real conflict resolver, branch icon affordance, and post-git refresh. ROC-1 iPhone addendum covers a brand-new Quick Chat passthrough repo session where the Agent Control Source Control card is omitted and no unsolicited no-worktree error appears. Branch picker visual/error coverage remains in the settings/navigation rows unless a later branch-specific failure appears. | Worktree status rows, git/worktree invocation rows, branch refs, protected branch policy, conflict resources, pending-merge origin, no-worktree/passthrough state, and classified error payloads. |
| Voice notes and attachments | ios_action_wiring | `AttachmentTests`, `DraftMetadataTests`, `DraftStoreTests`, `InputBarContentAreaChipTests`, `AudioAvailabilityMonitorTests`, `AudioRecorderTests`, `VoiceNotesRecorderTests`, `TranscriptionCoordinatorTests`, and `MediaClientTests` cover attachment metadata/storage, chip layout, recording lifecycle, availability, and transcription coordination. | UXR-4 will run unavailable/record/cancel/submit, transcription disabled/enabled, and attachment add/remove on iPhone. | `voice_notes::*` / `media::*` invocation rows, transcription settings truth, artifact/materialized file resources, draft attachment metadata, and surfaced failure events. |
| Prompt library, subagent, process/log/detail sheets | ios_state_projection | `SkillStoreTests`, `SkillClientTests`, `MentionDetectorTests`, `SkillBadgesTests`, `SkillDetailSheetTests`, `SubagentStateTests`, `SubagentTypesTests`, `SubagentChipVariantTests`, `SubagentCapabilityEventFormatterTests`, `ProcessStateTests`, `ProcessedEventItemTests`, `SheetCoordinatorTests`, `SheetReadOnlyTests`, `ChatSheetTests`, `ClientLogIngestionServiceTests`, `TronLoggerTests`, and `DiagnosticsBundleBuilderTests` cover mention/skill affordances, subagent lineage, process/log state, and detail sheet safety. | UXR-4/UXR-5/UXR-8 will open/close prompt, skill, subagent, process, log, and detail sheets on iPhone. | Prompt/snippet resources, subagent session/invocation rows, process invocation status/output resources, client log ingestion rows, and read-only sheet action gating. |
| Mac wizard/menu/logs/install/update/uninstall | mac_wrapper_ui | `WizardStateTests`, `WizardStepTests`, `InstallPlannerTests`, `ExistingInstallDetectorTests`, `MacAppStartupMaintenanceTests`, `MacCommandModeServerStarterTests`, `MenuBarItemBuilderTests`, `MenuBarActionHandler` coverage through `MenuBarControllerTests`, `ServerStatusPollerTests`, `ServerPingTests`, `ServerProcessProbeTests`, `DevServerStopperTests`, `TronUninstallerTests`, `MacRuntimeVariantTests`, `TranscriptionSetupTests`, `FeedbackComposerTests`, and `DiagnosticsRedactorTests` cover the pure planners, menu descriptors, status polling, dev takeover controls, command-mode start finalization, uninstall preservation, runtime split, transcription setup, and diagnostics redaction. | UXR-9 passed live local Mac verification for Debug companion, Stop Dev health-gated recovery failure, isolated install registration, `/health` on 9848, and permission-page projection. ROC-1 passed live production/local Release replacement, SMAppService/LWCR repair, command-mode start marker finalization, stop/start/restart, and command-mode uninstall preservation. | `/health`, launchd print, SMAppService state, helper PID/bundle path, update/finalization logs, uninstall preservation checks, and wrapper-visible status/error text. |

## UXR Scenario Ledger

| ID | Scenario | Raw Points | Status | Evidence | Open Loops |
|---|---|---:|---|---|---|
| UXR-0 | UI inventory and test harness | 5 | passed | 2026-05-30 inventory audit mapped every required UI surface above to concrete representative tests and DB evidence requirements. iPhone-only live scope for this session is explicit: iPad-specific visual coverage is deferred to a later plan/session by user request, while Mac wrapper coverage remains reserved for ROC-1/UXR-9. Static gate `post_100_operating_scorecard_stays_formalized` covers the scorecard link, scenario ids, owner taxonomy, and evidence-contract text. | None for UXR-0; continue with UXR-3 chat/engine visual parity on iPhone. |
| UXR-1 | Dashboard session cards and metadata icons | 10 | pending | 2026-05-30 iPhone checkpoint passed after fixes for owner `ios_state_projection` / `ios_rendering`: `SessionTitleIcons` now shows dirty dots for isolated on-base dirty worktrees while suppressing passthrough/non-isolated repo dirt, exposes matching accessibility descriptors, and keeps the icon cluster fixed-size. Sidebar preloads worktree status after the active server is connected instead of relying on per-row tasks, so relaunch shows all visible branch/fork/dirty indicators. Session cache schema v13 persists `is_processing`, and legacy provider-schema rebuilds now run before current-column migrations so processing state is not dropped. Simulator: iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`; screenshot `/tmp/uxr1-session-cards-iphone-20260530112912.png` covers plain, forked, dirty, isolated, fork+dirty, long-title/path, and narrow phone rows; `/tmp/uxr1-processing-active-iphone-20260530114402.png` covers live processing row `sess_019e7a33-49aa-71f2-9aca-b169634b18e2` while server `session::list` reported `isRunning=true`; Computer Use accessibility tree showed dirty/isolated rows with `branch 019e7a19, dirty worktree`. Tap-open path was verified by opening `UXR-1 clean plain card 20260530112912` and returning to the session list. Focused tests passed: `DatabaseSchemaTests` + `EventDatabaseTests/testSessionPersistenceRoundTripsProcessingFlag` (6 tests), `SessionTitleIconsTests` + `WorktreeStatusCacheTests` (40 tests). | iPad/sidebar live layout intentionally deferred by user on 2026-05-30 for a later plan/session. Archive/delete final UI mutation for disposable row `UXR-1 archive disposable card 20260530112912` remains pending action-time confirmation for Computer Use; the row exposes the `Archive` secondary action. |
| UXR-2 | Notification inbox actions | 10 | passed_after_fix | 2026-05-30 iPhone checkpoint passed after fixing notification read actions for owner `ios_action_wiring` / `server_contract`: `notifications::mark_read` and `notifications::mark_all_read` now return server global `unreadCount`; iOS uses that value instead of local optimistic arithmetic, disables action buttons while writes are in flight, surfaces toast failures, and clears the app badge only when the server unread count reaches zero. Notification resource child writes now inherit caller session/workspace context when present and use the notification system context for global inbox actions, removing the live `session-scoped idempotency requires a session id` failure. `sheetContent` is now a Markdown string contract matching native detail renderers. Simulator: iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`, seed stamp `20260530192346`; `/tmp/uxr2-notifications-iphone-20260530192346.png` shows the refreshed inbox after bell count/read actions, and `/tmp/uxr2-offline-failure-iphone-20260530192346.png` shows the disconnected Mark Read toast. DB evidence: manual `notifications::mark_read` `019e7a58-0361-7e41-bdbd-2b3eb4e43ab3` succeeded with session `sess_019e7a57-aa36-7f03-9abc-d5c25fb15354`, system idempotency scope, and `unreadCount=2`; deep-link session auto-read `notifications::mark_all_read` `019e7a58-c110-7ae0-804d-a5d2bb0d4b76` marked 1 and left `unreadCount=1`; global Read All `019e7a59-1cbd-79e0-be1e-01145af3cfa8` marked 1 and returned `unreadCount=0`; post-offline cleanup `notifications::mark_read` `019e7a5c-3248-7602-a1dc-6d92302f2380` succeeded with `unreadCount=0`. The seeded four notification resources all have read decision links, and there were 0 failed server invocations since the UXR-2 seed. Focused tests passed: `NotificationClientTests` + `NotificationStoreTests` (9 tests), `notification_resources` (8 tests), `notifications::tests` (4 tests), `threat_model_invariants` (65 tests), plus `cargo fmt --all -- --check`, `xcodegen generate`, and `git diff --check`. | iPad notification layout and sidebar coverage intentionally deferred by user on 2026-05-30 for a later plan/session. Historical failed invocations before the fix remain in the DB as pre-fix evidence only; no failed invocations occurred after the UXR-2 seed. |
| UXR-3 | Chat/engine visual parity | 12 | passed | 2026-05-30 iPhone checkpoint passed for owners `stream_or_reconstruction` and `ios_action_wiring`. Server PID `81065`, `/health` returned `status=ok`, active session `sess_019e7a6c-8a54-79f0-9c5d-81fba1a6163f`, simulator iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`. Harness seed `20260530124634` created a real session through `/engine` and submitted an `agent::prompt` that made two `filesystem::read_file` calls; Computer Use confirmed the visible user prompt, green completed README card, red expected missing-file card, assistant terminal marker `UXR-3 harness terminal marker 20260530124634`, and model chip, screenshots `/tmp/uxr3-harness-iphone-20260530124634.png` and `/tmp/uxr3-native-iphone-20260530124800.png`. Native typed prompt marker `20260530124800` was entered through the iPhone input bar and rendered with the exact assistant terminal reply. Approval route `20260530125300` used an agent-driven `capability::execute` request for `process::run`; Computer Use confirmed the pending high-risk sheet, denial through the sheet, visible denied card, and restored denied state after relaunch/deep link, screenshots `/tmp/uxr3-approval-pending-iphone-20260530125300.png`, `/tmp/uxr3-approval-denied-iphone-20260530125300.png`, and `/tmp/uxr3-relaunch-iphone-20260530124634.png`. DB evidence for the session: stream topics `agent.runtime=21`, `approvals=2`, `events.session=177`, `queue.lifecycle=9`; queue rows `completed=3`; approval `019e7a70-0e03-7773-a982-7bf71998b70c` ended `denied` by `engine-user`; `uxr3-agent-approval-20260530125300.txt` was not materialized; compact events `0`. Classified failures: expected missing `filesystem::read_file` for `UXR3_MISSING_20260530124634.txt`, plus two pre-test approval route corrections (`approval::request` hidden from `engine-client`, and a `process::run` schema rejection for unsupported `targetPath`). One direct trusted-client process probe materialized a scratch file before the agent-driven approval path was established; the file was removed and excluded from approval evidence. Focused iOS test `EngineApprovalStateTests/testPendingApprovalEventCreatesChipAndOpensSheet` was added so pending approval events must create the chat chip and open the sheet. Verification passed: `xcodegen generate`, selected iPhone `xcodebuild` targets `EngineApprovalStateTests`, `UnifiedEventTransformerTests`, and `ChatViewModelEventRoutingTests` (124 tests), `threat_model_invariants` (65 tests), and `git diff --check`. | iPad visual parity intentionally deferred by user on 2026-05-30 for a later plan/session. UXR-5 still owns approval approve/double-tap, stale terminal action rejection, generated UI action flow, and broader capability detail sheets. |
| UXR-4 | Input bar, attachments, voice notes, prompt submission | 8 | passed_after_fix | 2026-05-30 iPhone checkpoint passed after fixing simulator audio capture for owner `ios_action_wiring`: `AudioCaptureEngine` now uses a compile-time simulator-safe backend that preserves the real `prepare/start/stop/cancel` state machine and writes deterministic silent WAV data without touching `AVAudioEngine` input on simulator, while device builds keep the AVAudioEngine path. Session `sess_019e7a79-b142-7452-824b-3dfc2c5ef018`, seed `20260530130056`, simulator iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`. Computer Use confirmed the text prompt path, queued-while-streaming chip, queued prompt auto-drain, stop-agent interruption chip, attachment add/remove, skill mention popup, staged `@browse-the-web` chip, and chip removal; screenshots `/tmp/uxr4-session-open-iphone-20260530130056.png`, `/tmp/uxr4-queue-chip-iphone-20260530130056.png`, `/tmp/uxr4-queue-drained-iphone-20260530130056.png`, `/tmp/uxr4-stop-interrupted-iphone-20260530130056.png`, `/tmp/uxr4-attachment-added-iphone-20260530130056.png`, `/tmp/uxr4-attachment-removed-iphone-20260530130056.png`, `/tmp/uxr4-skill-mention-popup-iphone-20260530130056.png`, and `/tmp/uxr4-skill-chip-iphone-20260530130056.png`. The initial input-bar mic tap exposed a real simulator-only CoreAudio abort (`CoreAudio: Initialize: RPC timeout. Apparently deadlocked. Aborting now.`, crash report `TronMobile-2026-05-30-130806.ips`); after the fix, a disposable XCUITest harness pinned to the same UDID tapped the live input-bar mic, observed `Stop recording`, tapped stop, and the app stayed foreground with `No speech detected`, screenshot `/tmp/uxr4-mic-uitest-after-20260530130056.png`. The same harness opened the dashboard voice-note sheet, observed `Recording...`, tapped `stop.fill`, observed `Ready to save`, canceled, and returned to the Sessions dashboard, screenshot `/tmp/uxr4-voice-note-uitest-after-20260530130056.png`. DB evidence: queue rows for the session ended `completed=2`, recent invocation topics included `agent::queue_prompt`, `agent::prompt`, `agent::prompt_queue_drain`, and `agent::abort` succeeded; post-fix `transcription::audio` invocation `019e7a8d-abf8-70d2-9bde-21458444a56e` succeeded at `2026-05-30T20:22:47Z` with empty text from silent simulator audio, matching the no-speech chip. Verification passed: `xcodegen generate`, `xcodebuild build -scheme 'Tron Beta' -configuration Beta -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`, same-UDID focused `AudioCaptureEngineTests` (2 tests), selected audio/input iPhone suite (133 XCTest tests plus 2 Swift Testing `MediaClient` tests), disposable same-UDID UI smoke tests for input-bar mic and voice-note sheet, scorecard invariant `post_100_operating_scorecard_stays_formalized`, and `git diff --check`. | iPad input layout intentionally deferred by user on 2026-05-30 for a later plan/session. Native Computer Use could not attach to the Simulator window for the final mic retest after the hard restart (`cgWindowNotFound` despite same booted simulator), so the final audio retest used XCUITest plus `simctl` screenshots; treat that as a test-harness open loop, not a product failure. |
| UXR-5 | Capability cards, approval sheets, generated UI | 9 | passed_after_fix | 2026-05-30 iPhone checkpoint passed after fixing owner `ios_action_wiring`: approval submission no longer locally marks a chip approved/denied before engine truth. The chip now enters a read-only `resolving` state, `approval::resolve` owns the final approved/denied/failed render, non-pending sheets hide decision buttons, and resolving approvals cannot be resubmitted. Server PID `81065`, `/health` healthy, session `sess_019e7a9d-4922-7993-af7c-2f0e13b1cb21`, simulator iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`. Clean harness stamp `20260530133948` exercised `filesystem::read_file`, `process::run`, `resource::list`, `web::fetch`, `model::list`, and `settings::get` through `capability::execute`; DB rows show all six succeeded and the expected missing `filesystem::read_file` failed with `FILE_NOT_FOUND`. Computer Use opened process, filesystem, resource, web, model, settings, and missing-file detail sheets; screenshots `/tmp/uxr5-capability-process-detail-iphone-20260530133948.png`, `/tmp/uxr5-capability-cards-iphone-20260530204840.png`, and `/tmp/uxr5-capability-error-detail-iphone-20260530204840.png` cover the visible card stack and classified error detail. Approval approve/double-tap path used approval `019e7a9f-ee2a-7081-b326-9110653585f7`; Computer Use tapped Approve twice, DB shows exactly one `approval::resolve` with idempotency `ios:approval.resolve:019e7a9f-ee2a-7081-b326-9110653585f7:approve` and one approved `process::run` `uxr5-approval-approve-20260530133948`, with approval status `executed` and materialized output captured in `result_json`; screenshots `/tmp/uxr5-approval-approved-iphone-20260530133948.png` and `/tmp/uxr5-approval-readonly-iphone-20260530133948.png` show the resolved chip and read-only sheet with no approve/deny controls. Generated UI resource `ui-surface-uxr5-20260530133948` rendered in Engine Console; direct server flow created version `ver_019e7a9f-dc26-7b93-babf-ec487d733917`, submitted `create-snippet`, refreshed to `ver_019e7a9f-dc48-7b03-84ba-e0cd11b9d262`, and rejected stale submit `uxr5-ui-stale-20260530133948` before mutation; DB count for stale `STALE UI SHOULD NOT CREATE` children is `0`. Computer Use rendered the generated Prompt Snippets surface, submitted the form through iOS `ui::submit_action`, and refreshed the surface; screenshots `/tmp/uxr5-generated-ui-surface-iphone-20260530133948.png` and `/tmp/uxr5-generated-ui-submit-refresh-iphone-202605302052.png`. The long iPhone-typed detail prompt had keyboard/autocorrection noise in several idempotency tokens, so exact DB assertions use the clean harness/server keys and the typed prompt was used only for bottom-of-chat visible detail-sheet tapping. Verification passed: `xcodegen generate`; selected iPhone `xcodebuild` capability/approval/generated-UI suite (92 XCTest tests plus 25 Swift Testing tests); `cargo test --manifest-path packages/agent/Cargo.toml generated_ui --lib -- --nocapture` (24 tests); `cargo test --manifest-path packages/agent/Cargo.toml approval --lib -- --nocapture` (48 tests); targeted `threat_model_invariants` for generated UI, approval ownership, and scorecard formalization; `git diff --check`. | iPad capability/generated UI visual coverage intentionally deferred by user on 2026-05-30 for a later plan/session. Manual generated-UI form submission was clicked twice while validating refresh, producing two fresh iOS user-action snippet creates; stale-version rejection was separately proven to create zero stale children. |
| UXR-6 | Source control and worktree UX | 8 | passed_after_fix | 2026-05-30 iPhone checkpoint passed after fixes for owners `ios_state_projection`, `ios_action_wiring`, and `server_contract`: Source Control now uses one `sourceControlRefreshTick` for status, diff, and repo-divergence refresh; strict Worktree/Repo event plugins reject missing required payload fields instead of defaulting; `worktree.pending_merge_detected` now carries server `origin`; conflict banners drop unknown origins instead of coercing to finalize; Source Control disables merge/push until server git policy loads; and merge/rebase copy no longer assumes a local `main` default when the server owns the default branch. Server health returned `status=ok`, `uptime_secs=7272`, `active_sessions=3`; simulator iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`; disposable session `sess_019e7aca-ecfe-7641-96f0-57ed0dcec443`, repo `/tmp/uxr6-repo-5RWlRV`, local bare origin `/tmp/uxr6-origin-2waVV5.git`. Computer Use confirmed the Agent Control Source Control card shows the `arrow.triangle.branch` icon with dirty state, then executed the iPhone UI path: opened Source Control, committed `uxr6-dirty.txt` as `UXR-6 commit from iPhone`, pushed the session branch and set upstream, rebased on the base branch, merged/finalized into `main` and rebranched to `session/sess_019e7aca-ecfe-7641-96f0-57ed0dcec443-follow-up`, pulled a remote-only fast-forward, then triggered a real both-added rebase conflict and opened the resolver. Screenshots: `/tmp/uxr6-agent-control.png`, `/tmp/uxr6-source-control-dirty.png`, `/tmp/uxr6-after-commit.png`, `/tmp/uxr6-after-push.png`, `/tmp/uxr6-after-rebase.png`, `/tmp/uxr6-after-merge.png`, `/tmp/uxr6-pull-success.png`, `/tmp/uxr6-conflict-resolver.png`. DB events for the session: `worktree.acquired=1`, `worktree.commit=1`, `worktree.pushed=1`, `worktree.rebased_on_main=1`, `worktree.session_finalized=1`, `worktree.main_synced=1`, `worktree.merge_started=1`, `worktree.conflict_detected=1`, with conflict payload `origin=rebase_on_main`; repo locks acquired/released four times. DB invocations after scenario start: `worktree::commit`, `git::push`, `worktree::rebase_on_main` twice, `worktree::finalize_session`, `git::sync_main`, and `worktree::get_status` all succeeded; failed invocations after scenario start `0`. Logs after scenario start had no errors/fatals; the only relevant warning was the expected `git rebase main` conflict, plus one iOS WebSocket timeout after the conflict screenshot. Focused verification passed: selected iPhone source-control/event/settings `xcodebuild` tests, Rust `rebase_on_main` and `tron_catalog` tests, `cargo fmt --all -- --check`, `threat_model_invariants`, and `git diff --check`. | iPad source-control layout intentionally deferred by user on 2026-05-30 for a later plan/session. GUI abort/discard/delete/prune branches were not clicked because Computer Use destructive actions require action-time user confirmation; the conflict resolver and disabled conflicting action states were verified without triggering those destructive UI paths. |
| UXR-7 | Settings, providers, server pairing UX | 8 | passed_after_fix | 2026-05-30 iPhone checkpoint passed after fixing owners `ios_state_projection` and `ios_action_wiring`: Settings now projects `server.defaultModel` from `settings::get`; Agent model selection writes through `settings::update`, reloads `settings::get`, and updates app globals only after accepted server truth; Settings model pickers hide the reasoning selector unless the caller passes bound reasoning state; and Settings-launched server onboarding is deferred until the Settings sheet dismisses so SwiftUI does not drop the pairing modal. Disposable server HOME `/tmp/uxr7-home-lMQlp2`, port `9857`, final server PID `535` (tool session `97155`), `/health` `status=ok`, simulator iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`. Computer Use exercised the real iPhone flow: `tron://pair` opened onboarding Step 4 with host `127.0.0.1`, port `9857`, token masked, and label `UXR-7+Disposable`; Connect succeeded; workspace `/tmp/uxr7-home-lMQlp2` saved; Settings showed App/Server/Providers/Agent/Context/Plugin Sources plus enabled Clear Prompt History and disabled Archive All when no sessions existed; App Settings toggled Auto-mark as Read; Providers added and edited fake Anthropic key label `uxr7`, then showed `sk-ant-...ited`; seeded disposable OAuth accounts rendered `Logged in with OAuth`, `OAuth will refresh`, and `OAuth expired`; Agent changed model to `Claude Opus 4.6` and added protected branch `uxr7-protected`; Server showed pairing/transcription/update/diagnostics controls; bad-token pairing reported `Wrong pairing token`; stopped-server state hid server-backed categories and disabled server-coupled destructive actions; Retry restored the full grid; Settings > Server > Connect to a new server opened onboarding after dismissal. Profile truth in `/tmp/uxr7-home-lMQlp2/.tron/profiles/user/profile.toml`: `server.defaultModel = "claude-opus-4-6"`, `server.defaultWorkspace = "/tmp/uxr7-home-lMQlp2"`, and protected branches include `uxr7-protected`. Redacted auth truth contains Anthropic label `uxr7` with final key hint `sk-ant-...ited` and OpenAI Codex OAuth accounts `uxr7-fresh`, `uxr7-refresh`, and `uxr7-dead`; latest `auth::get` showed fresh unexpired with refresh token, expired with refresh token, and expired without refresh token. DB evidence after scenario start: `auth::get=3`, `auth::update=2`, `settings::get=10`, `settings::update=3`, `model::list=4`, `device::register=6`, `logs::ingest=51`, `engine::invoke=109`, `system::ping=1`, and 0 failed invocations; logs had no fatal rows and one expected offline iOS WebSocket error after the deliberate server stop. Screenshots: `/tmp/uxr7-providers-iphone-20260530221153.png`, `/tmp/uxr7-pairing-from-settings-iphone-20260530221217.png`, `/tmp/uxr7-server-unavailable-iphone-20260530221257.png`. Verification passed: pairing/server settings slice (103 Swift Testing), broad UXR-7 iPhone suite (40 XCTest plus 236 Swift Testing), final focused iPhone diff rerun for `SettingsStateTests`, `ModelPickerStateTests`, and `ServerSettingsPageTests` (10 XCTest plus 41 Swift Testing), `cargo test --manifest-path packages/agent/Cargo.toml --test threat_model_invariants -- --nocapture` (65 tests), and `git diff --check`. | iPad settings/provider/pairing coverage intentionally deferred by user on 2026-05-30 for a later plan/session. API-key clear and protected-branch removal were not clicked because Computer Use destructive actions require action-time user confirmation; disposable auth/profile data remains inside `/tmp/uxr7-home-lMQlp2`. The onboarding-carousel Computer Use open loop was closed by UXR-10: explicit Back/Next controls now advance setup pages without relying on a hidden swipe gesture. |
| UXR-8 | Navigation, deep links, session tree, history | 7 | passed_after_fix | 2026-05-30 iPhone checkpoint passed after fixing owners `stream_or_reconstruction`, `server_contract`, and `ios_state_projection`: `session::reconstruct` now uses event-id pagination (`beforeEventId` / `oldestEventId`) and forked sessions reconstruct from the ordered ancestor chain ending at the child head; `tree::get_ancestors` returns resolved wire `events`; deep links support `tron://session/<id>?capability=<id>`, `tron://session/<id>?event=<id>`, and notification URLs; target resolution pages older history until the target event/capability is visible; and target navigation suppresses bottom auto-scroll/new-content state while history is prepended. Disposable server HOME `/tmp/tron-uxr8-home.rgFRA5`, port `9858`, tool session `76367`, `/health` healthy, database `/tmp/tron-uxr8-home.rgFRA5/.tron/internal/database/tron.sqlite`, simulator iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`. Seeded sessions `sess_019e7b01-fb0e-7352-8b3b-3556002c0447` (240 messages, 244 events), `sess_019e7b01-fb14-7cf0-bbc6-087a1a762d53` (dashboard card), and two fork sessions. DB evidence after the UXR-8 seed: sessions `4`; event types `message.user=121`, `message.assistant=121`, `session.start=2`, `session.fork=2`, `rules.loaded=2`, `capability.invocation.started=1`, `capability.invocation.completed=1`; engine invocations `1237/1237` succeeded including `session::reconstruct=42`, `tree::get_ancestors=4`, and `session::fork=2`; logs contained five classified error rows, one pre-fix ancestor decode failure, two socket disconnects during app/test restarts, one disconnected skill refresh after a test reinstall, and one transient `notConnected` during the first post-test deep-link retry. Computer Use and `simctl openurl` verified dashboard card selection/back, session deep link, capability target deep link, cold-start event target deep link to prompt 018, load-earlier pagination, notification detail deep link, history sheet fork button, successful fork rendering inherited history, and the first dashboard card's fork/branch icon. Screenshots: `/tmp/tron-uxr8-evidence/iphone-dashboard.png`, `/tmp/tron-uxr8-evidence/iphone-dashboard-card-opened-secondary.png`, `/tmp/tron-uxr8-evidence/iphone-session-deeplink-primary.png`, `/tmp/tron-uxr8-evidence/iphone-capability-deeplink-target.png`, `/tmp/tron-uxr8-evidence/iphone-load-earlier-visible.png`, `/tmp/tron-uxr8-evidence/iphone-load-earlier-after.png`, `/tmp/tron-uxr8-evidence/iphone-notification-deeplink-detail.png`, `/tmp/tron-uxr8-evidence/iphone-history-expanded-fork-button.png`, `/tmp/tron-uxr8-evidence/iphone-fork-success.png`, `/tmp/tron-uxr8-evidence/iphone-dashboard-first-card-branch.png`, `/tmp/tron-uxr8-evidence/iphone-cold-start-event-deeplink-target.png`, `/tmp/tron-uxr8-evidence/iphone-cold-start-event-deeplink-target-after-cleanup.png`, `/tmp/tron-uxr8-evidence/iphone-event-deeplink-foreground-final.png`, and `/tmp/tron-uxr8-evidence/iphone-cold-start-event-deeplink-target-post-tests.png`. Focused verification passed before checkpoint: Rust `e2e_reconstruct_fork_returns_ordered_ancestor_chain` and `e2e_tree_get_ancestors_returns_wire_events`; iPhone `xcodebuild` targets `ConnectionCoordinatorTests`, `ScrollStateCoordinatorTests`, `UnifiedEventTransformerTests`, `UnifiedEventTransformerReconstructionOrderTests`, `ChatViewModelFindMessageTests`, and `DeepLinkRouterTests`; `threat_model_invariants`; plus `cargo fmt` and `git diff --check`. | iPad navigation/layout testing intentionally deferred by user on 2026-05-30 for a later plan/session. The onboarding carousel swipe was bypassed with persisted `onboardingComplete=true` after pairing evidence because UXR-8 is not an onboarding-carousel lane. Nonzero `simctl openurl` commands were required during manual checks; the final post-test cold-start event deep link passed after the event-id cleanup and project/test split. |
| UXR-9 | Mac wrapper UX | 8 | passed_after_fix | 2026-05-30 Mac checkpoint passed after fixes for owner `mac_wrapper_ui`: start-like wrapper actions now wait for `/health` after ServiceManagement reports loaded; update finalization records `mac-app-version.json` only after healthy `/health`; `--tron-start-server-and-quit` exits nonzero when loaded-but-unhealthy; Debug companion remains read-only for production controls; isolated install uses resolved Tron home for Rust DB policy; the isolated LaunchAgent has its own `Tron Server Dev.app` helper with bundle id `com.tron.server.dev`; active LaunchAgent plists validate `BundleProgram`, environment, and associated wrapper order; the Xcode copy phase signs every nested helper and then re-signs the outer wrapper so ServiceManagement does not reject copied plists with `-67054`. Live evidence: default Debug companion with `tron dev -bd --json --wait 30` healthy on PID `78580` showed `Stop dev server` enabled and `Pause server`, `Restart server`, and `Uninstall Tron` disabled. Earlier live Stop Dev recovery against stale `/Applications/Tron.app` showed visible alert `Resume failed` instead of false success when ServiceManagement loaded an unhealthy installed helper. Isolated wizard live path in `TronMac.app` registered `com.tron.server.dev`, `launchctl print` showed state `running`, parent `com.tron.mac.dev`, `program identifier = Contents/Library/LoginItems/Tron Server Dev.app/Contents/MacOS/tron`, PID `74512`; `curl http://127.0.0.1:9848/health` returned ok with uptime; `lsof` showed PID `74512` listening on 9848; DB files were under `/Users/<USER>/.tron-dev/internal/database/tron.sqlite*`; Computer Use confirmed the wizard reached `Tron Server is ready Current status: Running on port 9848`, then advanced to the Permissions page naming `TronMac.app`. Final verification passed after the signing-script adjustment: `xcodebuild test -scheme TronMac -destination 'platform=macOS'` focused suite passed 112 tests at `/Users/<USER>/Library/Developer/Xcode/DerivedData/TronMac-fjgdtjmmbndhtwfqnfyaagtxvdvs/Logs/Test/Test-TronMac-2026.05.30_18-08-04--0700.xcresult`; normal Debug `xcodebuild` succeeded; `codesign --verify --deep --strict` validated the built wrapper and both helper apps; isolated launchctl env was unset, `com.tron.server.dev` was unloaded, and `/health` on port 9847 remained healthy. | Production/local Release replacement, update finalization, command-mode start, and uninstall preservation coverage were closed by ROC-1. Feedback issue submission and menu-bar uninstall destructive option clicks were not clicked live because Computer Use requires action-time confirmation for external data transmission/destructive UI; unit tests cover the redacted composer and preservation paths. The Permissions page stopped with expected missing Debug wrapper TCC grants; OS security settings were not changed. |
| UXR-10 | Visual QA and accessibility sweep | 5 | passed_after_fix | 2026-05-30 iPhone checkpoint passed after fixing owners `ios_rendering` and `ios_action_wiring`: first-run onboarding setup pages now have explicit Back/Next controls in the footer, `OnboardingState` owns bounded forward/back navigation, and setup pages remain locked until pairing succeeds; the dashboard plus affordance is now a real `Button` with accessibility label `New Session` and hint `Opens the new session sheet`; the voice-note affordance now exposes `Voice Note` with available/unavailable hints; decorative New Session card icons are hidden from accessibility; New Session opens at the large iPhone detent so lower setup cards and the Create toolbar action are visually reachable; and dashboard empty-state captions use the secondary text token for dark-mode readability. Simulator: iPhone 17 Pro UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`, `/health` on port `9847` returned `status=ok`, `connections=1`, `active_sessions=0`; app was restored to light appearance and `large` content size after testing. Computer Use verified the real iPhone flow: pairing deep link prefilled onboarding, Connect succeeded, explicit Next advanced Workspace -> Anthropic -> OpenAI -> Other Providers -> Search Services -> Default Model, Finish setup returned to the dashboard, plus opened New Session, Voice Note/New Session buttons had explicit labels, New Session rendered at large detent under `accessibility-extra-extra-large`, dark-mode dashboard/New Session/Settings had no overlap or clipped primary controls, and destructive/transmission buttons were not clicked. Screenshots: `/tmp/uxr10-dashboard-iphone-large-202605301812.png`, `/tmp/uxr10-dashboard-connected-iphone-large-202605301824.png`, `/tmp/uxr10-dashboard-iphone-axxxl-202605301825.png`, `/tmp/uxr10-new-session-iphone-axxxl-large-detent-202605301829.png`, `/tmp/uxr10-dashboard-iphone-dark-axxxl-after-contrast-202605301831.png`, `/tmp/uxr10-new-session-iphone-dark-axxxl-202605301831.png`, `/tmp/uxr10-settings-iphone-dark-axxxl-202605301832.png`, and `/tmp/uxr10-dashboard-iphone-restored-light-large-202605301832.png`. Focused verification passed: `xcodebuild test -scheme Tron -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:TronMobileTests/OnboardingStateTests -only-testing:TronMobileTests/AccessibilityTests -only-testing:TronMobileTests/NewSessionFlowTests` (22 XCTest plus 28 Swift Testing) at `/Users/<USER>/Library/Developer/Xcode/DerivedData/TronMobile-eqctauwqsqxkqyelqqpembdspvdk/Logs/Test/Test-Tron-2026.05.30_18-36-35--0700.xcresult`; `xcodegen generate` and Beta simulator `xcodebuild` passed. DB evidence since `2026-05-31T01:12:00Z`: 0 error/fatal logs, 522/522 invocations succeeded, no sessions created, and warning-only rows were expected simulator WebSocket reconnect/test-relaunch attempts. | iPad visual/accessibility coverage intentionally deferred by user on 2026-05-30 for a later plan/session. Settings destructive actions (`Clear Prompt History`, `Reset All Settings`, archive) and external feedback submission were inspected only for visible/accessible state and were not clicked because Computer Use requires action-time confirmation for destructive or data-transmission UI. |

UXR-8 final checkpoint addendum (2026-05-30): the focused iPhone rerun after the
last scroll-state fix passed at
`/Users/<USER>/Library/Developer/Xcode/DerivedData/TronMobile-eqctauwqsqxkqyelqqpembdspvdk/Logs/Test/Test-Tron-2026.05.30_16-45-48--0700.xcresult`.
After that test reinstall/reset cycle, the final cold-start event deep link
landed on prompt 018 in the actual Simulator app; screenshot
`/tmp/tron-uxr8-evidence/iphone-cold-start-event-deeplink-target-final2.png`.
iPad coverage remains intentionally deferred for this plan by user request.

UXR-1 archive checkpoint addendum (2026-05-31): iPhone-only destructive-action
staging used two disposable sessions on simulator UDID
`267F6468-09AE-471D-9157-29144173EB82`, bundle `com.tron.mobile.beta`.
Disposable `sess_019e7bb6-cc24-73f3-af61-405d5e7b8245` was created through
`session::create` with title `UXR-1 archive disposable card 20260531014717`,
Computer Use confirmed the dashboard row exposed the `Archive` secondary
action, screenshot `/tmp/uxr1-archive-row-before-20260531014717.png`, and a
server-owned `session::archive` cleanup returned `archived=true`; active
`session::list` no longer included the row, archived `session::list` returned
`isArchived=true` and `endedAt=2026-05-31T01:49:18.680519+00:00`, the iPhone
dashboard returned to the empty state after relaunch, screenshot
`/tmp/uxr1-archive-row-after-20260531014717.png`, and the simulator local
projection database no longer contained that session id. Disposable
`sess_019e7bba-558f-79e2-8087-12e391fc982f` was then created specifically for
the real Computer Use context action; Computer Use performed the row's
`Archive` secondary action and verified the native `Archive Session`
confirmation with `Cancel` and destructive `Archive` buttons. The final red
Computer Use `Archive` click was not performed because destructive UI actions
require action-time user confirmation and no confirmation arrived; that
session was cleaned up through server-owned `session::archive`. DB evidence for
the addendum: UXR-1 `session::create`, `session::archive`, and `session::list`
invocations succeeded with zero failed UXR-1 invocations, and logs since
`2026-05-31T01:47:17Z` had zero error/fatal rows. UXR-1 remains unscored until
the final destructive Computer Use confirmation click is explicitly authorized
or replaced by a committed same-UDID UI automation harness.

## Baseline Evidence

ROC-0 baseline was recorded on 2026-05-30:

- `scripts/tron ci fmt check clippy test`: passed after formatter correction to
  `packages/agent/tests/threat_model_invariants.rs`.
- `xcodegen generate`: passed in `packages/ios-app`.
- Focused iOS simulator smoke: `xcodebuild test -scheme Tron -destination
  'platform=iOS Simulator,name=iPhone 17 Pro'
  -only-testing:TronMobileTests/ContentViewCoordinatorTests
  -only-testing:TronMobileTests/PendingSessionDeepLinkTests` passed 7 tests.
- `scripts/tron dev -bd --json --wait 30`: passed, PID 22061, database
  `/Users/<USER>/.tron/internal/database/tron.sqlite`.
- `curl -fsS http://localhost:9847/health`: `status=ok`, `active_sessions=0`.
- Simulator evidence: UDID `267F6468-09AE-471D-9157-29144173EB82`, bundle
  `com.tron.mobile.beta`, screenshot `/tmp/roc0-baseline-simulator.png`;
  Computer Use confirmed the visible Sessions list and notification/settings
  affordances.
- DB classification since `2026-05-30T18:06:09Z`: 0 error/fatal log rows and
  0 failed invocations. Warning-only rows were simulator WebSocket attempts to
  an older paired host and were not classified as dev-server startup failures.

## Assumptions

- UXR-1 and UXR-2 are the first UI implementation lanes because they match the
  currently observed regressions.
- UI/UX correctness means visible app state matches engine/server truth; Swift
  does not own separate policy or product-state side channels.
- Simulator evidence is sufficient for iOS foreground UI. iPhone simulator
  coverage is the active scope as of 2026-05-30; iPad-specific layout coverage
  is intentionally deferred to a later plan/session by user request. APNs
  delivery still requires physical-device testing and should be marked blocked
  if no device is available.
- Mac UI scenarios may require local manual-safe app launching, but they still
  need `/health`, launchd, and SMAppService evidence before passing.
