# Quick Chat Tron profile.
#
# Chat sessions are conversational and not project scoped by default. The
# profile inherits the managed boot settings and only names the chat class; the
# primitive prompt loop does not use profile-specific entrypoints.

version = "3"
name = "chat"
managed = true
profileClass = "chat"
inherits = ["default"]
authProfile = "default"
