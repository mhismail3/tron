# Local-provider Tron profile.
#
# Local-provider sessions inherit the managed boot settings and name the local
# class. Provider/context adaptation now belongs to the primitive model loop and
# provider clients, not profile entrypoint policy.

version = "3"
name = "local"
managed = true
profileClass = "local"
inherits = ["default"]
authProfile = "default"
