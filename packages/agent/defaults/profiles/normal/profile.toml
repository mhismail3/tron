# Normal Tron profile.
#
# This is the standard project/workspace profile. It inherits the managed
# default boot settings and only names the user-facing profile class.

version = "3"
name = "normal"
managed = true
profileClass = "normal"
inherits = ["default"]
authProfile = "default"
