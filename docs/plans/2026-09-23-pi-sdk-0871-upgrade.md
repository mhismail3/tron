# Pi SDK 0.87.1 integration

- **Started:** 2026-09-23
- **Status:** Active
- **Last updated:** 2026-09-23, candidate validation checkpoint
- **Goal:** Upgrade Tron’s pinned Pi runtime to the latest verified SDK and account for every relevant release delta with correct integration, evidence, or an explicit disposition.

## Goal and constraints

Implementation and validation were performed on a dedicated worktree/branch with independent `node_modules`; integration to `main` is now explicitly user-approved. Preserve canonical runtime JSONL, settings, credentials, and packages. Do not change the running Gateway, use live credentials, or open canonical sessions with another runtime. No Gateway lifecycle/deployment transition is authorized to agents.

Use the existing `npm run update:pi-sdk -- <exact-version>` workflow; never hand-edit lockfiles or introduce unrelated upgrades. Keep canonical runtime JSONL, settings, credentials, packages and resources authoritative. No mirror runtime, generic compatibility shim, or speculative UI. Any behavior delta that changes intended events, persistence, prompts, model behavior or UX is a stop for explicit product review.

“Full support” means every applicable upstream change has one disposition: inherited with targeted evidence; Tron adaptation implemented/tested; obsolete workaround removed; optional/product decision deferred with reason and acceptance; not applicable with owner-boundary rationale; or unresolved blocker. Compilation alone is insufficient. Keep the concise recurring SDK boundary map in the Gateway maintenance docs; keep this candidate’s full version/change matrix here until closeout.

## Context

Two read-only audits agree the latest published family is 0.87.1 (`f07218c4d4bbc12bef056a7058c3dd49dfe41abe`), versus Tron’s 0.84.4. Node floor is `>=22.19.0`; project Node 22.22.0 meets it. Public-change anchors needing particular attention: 0.86 `TranscriptContext` custom stream contract; 0.87 persisted `ContextEditEntry` and new extension settlement boundaries; 0.87.1 native Anthropic Opus 5.5 and OAuth version. Candidate’s new `@earendil-works/chord` transitive graph replaces some previous direct coding-agent transitive packages, so verify current pin checker/helper assumptions from actual npm manifests.

Audit artifacts for the full six-release matrix and downstream seam map are in the parent session’s task context. Recheck evidence against the candidate tree; audits are not proof of runtime behavior. Preserve the unrelated untracked `docs/plans/2026-09-23-abandoned-login-cleanup.md`.

## Tasks

| ID | Status | Scope | Depends on | Owner |
| --- | --- | --- | --- | --- |
| SDK-1 | Done | Verify npm latest, activate this plan, claim and create isolated worktree | none | worker, 2026-09-23 |
| SDK-2 | Done | Upgrade pins using repository helper; reconcile package-family checks and baseline | SDK-1 | worker, 2026-09-23 |
| SDK-3 | Done | Integrate required stream, persisted-context, extension-boundary, model and runtime deltas | SDK-2 | worker, 2026-09-23 |
| SDK-4 | Done | Evaluate CortexKit and complete maintenance boundary docs and delta dispositions | SDK-2, SDK-3 | worker, 2026-09-23 |
| SDK-5 | In progress | Integrate approved candidate and fixes; prepare artifacts; retain manual acceptance gates | SDK-2, SDK-3, SDK-4 | worker, 2026-09-23 |

## Task details

### SDK-1 — Verify, activate, claim, isolate

Use the repository work-plan protocol. Verify npm `latest` and all-family preflight; freeze 0.87.1 only if still latest. Commit plan Proposed, then Active after explicit user approval, then claim SDK-1 on `main`. Create candidate branch/worktree from the resulting main commit, with a real independent install (no main/live dependency symlinks). Do not stage unrelated files.

### SDK-2 — Pin and package graph

Use `npm run update:pi-sdk -- 0.87.1` in the isolated candidate worktree only. Run `check:pi-sdk`, SDK helper tests, graph comparison, and inspect actual npm dependency/shrinkwrap/export/engine/integrity graph. Adjust checker/helper and its tests only if actual candidate family semantics require it; preserve safety invariants. Update rollback baseline only per its documented previous-runtime meaning. Prove CLI symlink, native runtime alias and candidate node floor; do not stage generated payloads unless a packaging validation requires disposable staging.

### SDK-3 — Required integration delta

Audit candidate types/docs/examples and implement minimal complete adaptations at owned seams, with focused regressions and owner docs:

- **TranscriptContext (0.86):** identify every custom `streamSimple`/provider wrapper, message/tool conversion and JSON-result boundary. Update only Tron-owned code to supported transcript APIs. Do not patch installed user packages.
- **ContextEditEntry (0.87):** preserve SDK model-context semantics and canonical entry order through Gateway admission/validation, branch projection, session history, snapshots, import/export, compaction/resume/fork. Keep raw transcript/history intact; do not render it as a fabricated user message. Prove the canonical next provider context through restart/resume and branch changes. Gateway imports must use an SDK-supported persistent fork API to create a fresh identity; never replace the source session.
- **Extension lifecycle (0.87):** explicitly disposition all new event/settlement/context APIs in compatibility manifests and adapters. Preserve admission, serialization, cancellation, append ordering and canonical terminal receipts. Expose new mutating capabilities only if the existing owner can uphold those guarantees; otherwise document a precise supported limitation and request a decision if it blocks accepted behavior.
- **Model/media (0.87.1):** verify generated native Anthropic Opus 5.5 metadata, adaptive thinking, long-context limits and requests through existing runtime/catalog contracts. Qualify SDK per-model image resize limits against attachments, tool-read images, compaction and canonical source data; avoid duplicate resizing or changing source/attachment identity.
- **Inherited runtime behavior:** test affected retry/compaction/cancel, tool results, extension settlement, auth/provider registration and ordering against Gateway owners. Search removed APIs after pin; no speculative hook use.

### SDK-3 acceptance

Tests demonstrate old session raw history remains unchanged while branch-relative context edits alter exactly the SDK context sent to the provider, including omitted/replaced messages across restart/fork/import/export/compaction. Extension callback/settlement order and durable completion projections are verified. Opus 5.5 appears in the built-in catalog and Gateway `model.list`; deterministic Anthropic wire/auth refresh and full media/tool boundary remain. Any observed user-visible behavior change that is not clearly an upstream correctness fix stops for decision, not silent adoption.

### SDK-4 — External packages and recurring workflow

Use isolated disposable package copies/fixtures to assess installed `pi-sub-anthropic@0.1.4` and the user-selected exact `@cortexkit/pi-anthropic-auth@1.23.0` against SDK peer/runtime contracts. Do not read/edit live credentials or mutate the user package installation. Record compatibility evidence and failure boundary; extension installation remains a separate explicit action. Do not enable relay, fallback accounts/API routes, cache warming/priming, request dumps or fast mode by default.

Add a compact recurring map to `packages/gateway/README.md` covering SDK-owned Gateway integration surfaces and the evidence owners, plus the workflow requiring per-release delta inventory/dispositions. Update nearest behavior docs. Keep detailed release matrix/dispositions in this active plan until closeout. Do not add a parallel tracker, upgrade framework, generic adapter, or claim unsupported optional CLI/UI capabilities as parity gaps.

### SDK-5 — Validation and handoff

Candidate validation and user-approved integration are complete. Remaining SDK-5 work is manual live acceptance (provider OAuth/login-refresh and device/account checks), qualification of any arbitrary project extensions, and user adoption/install. Use `.agents/skills/tron-ios/SKILL.md` for iOS build/test. Agents may prepare artifacts but never install on devices or transition Gateway.

## Change matrix

| Upstream delta | Tron disposition | Owner/evidence |
| --- | --- | --- |
| 0.85.0 agent-core turn-boundary semantics; import/fork/concurrent-share fixes | Verify inherited behavior; adapt only direct API use | Runtime-slot/invocation/session tests |
| 0.85.0/0.85.1 provider stream/API and catalog fixes; GPT-6 Astra; package export correction | Inherited where supported; root exports only | Provider/catalog tests; SDK export checker |
| 0.86 TranscriptContext custom stream API break and JSON-only/readonly tool values | Tron compaction wrapper appends a request-local system delta while preserving current system/tools; pass-through wrappers unchanged. External `pi-sub-anthropic@0.1.4` reads removed `context.systemPrompt/tools`; isolated request capture proves it omits both under 0.87.1. Package change/replace remains a separate user decision. | `compaction-policy.test.ts`; Node 22 external-package body-capture fixture |
| 0.86 provider retry/thinking/signature/context-overflow and cancellation fixes | Inherited; prove owning behavior | Provider, retry, compaction and abort tests |
| 0.86 cache lifetime/prompt cache warming | Warming deferred: additional network/usage effects need product policy | No enablement without explicit control, bounds, cancel/accounting tests |
| 0.86 per-model compaction budgets | Optional/deferred unless existing Gateway settings owner maps naturally | No duplicate settings state; acceptance needed before UX |
| 0.86 Radius, deferred Fireworks tools, fallback overrides, bug uploads, TUI updates | Not applicable or product/security review; no automatic exposure | No current Tron owner; classify after candidate API check |
| 0.86.1 Meta OAuth/provider | Inherited plumbing, usability/security qualification required before claim | Auth broker/catalog tests; privacy review if user-facing |
| 0.87 ContextEditEntry/canonical SessionManager context | Adapted: Gateway retains canonical edits without fabricating chat rows; SDK-backed provider capture proves replace/reopen/branch/fork/import/export/compaction semantics. Found and fixed Gateway import's empty in-memory session-directory failure by creating import-as-new with SDK `SessionManager.forkFrom`; repeated imports retain independent canonical IDs and preserve source entry IDs/parents. | `context-edit.integration.test.ts`; RuntimeRegistry export/import, callback-order and settlement receipt fixture; Session History projections/native tests |
| 0.87 turn_end/agent_before_settle/context_with_system settlement | New SDK-owned events classified `pi-runtime`; no Gateway event bridge added because Pi runs callbacks in-session. The SDK docs (`docs/extensions.md`, `agent_start--agent_end--agent_before_settle--agent_settled` and event-order section) define `agent_before_settle` as actionable with one continuation and `agent_settled` as final/notification-only; declarations model continuation results only for `turn_end` and `agent_before_settle`. Candidate notification fixtures now use that supported boundary and record `agent_start → agent_end → agent_start → agent_end → agent_settled`, exact assistant source IDs, and the retained completed receipt of the preceding user invocation. `pi.sendMessage(..., { triggerTurn: true })` remains available from `agent_settled`, but is a separate SDK run/operation, not equivalent continuation. Bounded inventory found no old-style continuation consumer in installed packages: pi-subagents uses `agent_settled` for widget resumption and its triggerTurn use is in `session_compact`; pi-web-access triggers from async fetch/command paths; no `agent_settled` consumer found in pi-goal, browser, or pi-sub-anthropic. Arbitrary project extensions remain to be checked if present. No shim is provided. Context-edit settlement fixture verifies delivery to the next provider request and durable attention completion revisions. `context_with_system` direct transformation remains inherited without an owned caller. | `compatibility-manifest.ts`; RuntimeRegistry context-edit and terminal-notification integration tests; SDK docs and declarations |
| 0.87 finishTurn/prepareRequest/queue peek; removed shouldStopAfterTurn | Search and map; no new hooks without need | Build and runtime owner tests |
| 0.87 per-model image resize and conversion fixes | Adapted: Pi owns initial resize by active model profile for prompt attachments and built-in/tool result paths; removed Tron pre-resize that could double-transcode before a stricter per-model profile. Canonical prompt image uses the exact 1,000 px profile while caller source bytes remain unchanged. A bounded temporary RuntimeRegistry/faux-provider test also proved local read-tool result images receive the same model profile (max side 1,000 px) while the source file remains 1,320×2,868. A deterministic Anthropic `fetch` capture with a fake key (no network) preserved an image through provider conversion. `maxRequestBytes`/per-message and per-request image ceilings are descriptive SDK metadata not enforced by Pi 0.87; no duplicate Gateway policy added. | Committed RuntimeRegistry prompt-image test; Node 22 temporary local read-tool image smoke; Node 22 fake-fetch Anthropic request capture; SDK `agent-session.js`/`docs/models.md`; attachment source remains upload-owned |
| 0.87.1 native Anthropic Opus 5.5, adaptive thinking, 1M metadata, Claude version 2.1.280 | SDK native catalog inherited; focused regression verifies 1M context, 128K output, adaptive thinking and mid-conversation metadata. Gateway `model.list` test verifies Opus 5.5 capacity. A bounded no-network `fetch` capture using a fake API key verified request `thinking.type=adaptive`, high effort, system prompt, tool schema, image forwarding, and model max-token cap. | `context-window-policy.test.ts`; `gateway-context-window.test.ts`; Node 22 fake-fetch Anthropic request capture; no live credentials/network |
| 0.87.1 OpenAI/Codex GPT-6, Copilot and Grok additions/fixes | Inherited catalog/provider behavior; no unrelated UX expansion | Refresh/model catalog tests |
| 0.85–0.87.1 in-memory/SQLite/Pico/Chord/remote Pi client, CLI/TUI features | Not applicable to Tron canonical JSONL, Gateway RPC or native UI | Preserve ownership boundaries; SDK graph/packaging tests |
| SDK package graph: chord addition, previous transitive graph changes | Required maintenance-tool update only if proven by actual published graph | Update helper/coherence/graph script tests and exact lock evidence |
| User-selected CortexKit replacement package | `@cortexkit/pi-anthropic-auth@1.23.0` peer range admits SDK >=0.86.1; disposable Pi 0.87.1 loader fixture registers the intended `anthropic` override, follows `PI_CODING_AGENT_DIR`, and package converter preserves normalized system prompt, tools and messages. Standard OAuth hooks return credentials through Pi's auth interface; no live credentials/network used. Sidecar untouched at load; relay/fallback/API routes/cache warming/priming/dump/fast mode not enabled. Extension installation remains deferred; no actual login/refresh or model request tested. | Npm integrity `sha512-0Bwo...`; disposable Pi/Jiti loader and converter smoke; exact peer metadata; read-only package audit |
| 0.87.1 SDK custom stream vs external installed `pi-sub-anthropic@0.1.4` | Incompatible at runtime despite TS typecheck: plugin treats TranscriptContext as old Context; captured request omits system prompt and tools. Do not patch/swap user package; separate package update/replacement needs user approval. | Disposable npm tarball copy; Node 22 request capture `{system:null, tools:null}`; no credentials/network |

## Handoff log

### SDK-5 · Integrated / manual acceptance pending · 2026-09-23 · worker

- Result: User explicitly approved integrating `fix/pi-sdk-0871-upgrade`; merged SDK 0.87.1 and the iOS OAuth fallback/image prompt display fixes into `main`. The running Gateway was already manually adopted at the validated SDK payload before these latest source changes. No deployment or device install was performed.
- Changes: See merge commit and prepared artifact provenance in the completion handoff. The staged-update-from-iOS plan remains Proposed and uncommitted; no automatic deployment or update policy was implemented.
- Acceptance pending: CortexKit live OAuth/login/refresh; arbitrary project extension qualification; user adoption of newly prepared Gateway payload and installation of the physical development iOS build.
- For the next agent: Preserve this plan as Active until manual acceptance is reported; then close it with actual evidence and remaining limitations.

### SDK-3/SDK-4 · Checkpoint · 2026-09-23 · worker

- Result: Added SDK-backed context-edit coverage for provider input after reopen, branch-relative omission, fork, JSONL-copy import, and compaction summary. Gateway import no longer constructs an in-memory manager whose session directory is empty: it uses `SessionManager.forkFrom` for fresh persistent identity, verifies the ID is not already registered, retains all source entry IDs/parent pointers/context edits, and removes only its new file on runtime setup failure. Two imports of one still-open source receive distinct IDs without replacing the source. The unused `RuntimeSlot.importFromJsonl` shim was removed.
- Event evidence: Extension fixture records `turn_end` → `agent_before_settle` → `agent_settled`, applies a context edit to the next provider call, and asserts Gateway attention completion revisions after each settled turn.
- Media evidence: Pi 0.87 applies `model.inputLimits.images.resize` in `_normalizePromptImages` before history admission. Gateway's `boundPromptImages` was redundant and could transcode twice for a stricter per-model limit, so removed that helper; RuntimeRegistry regression uses a 1,000 px profile and verifies exact canonical dimensions while retaining original caller bytes.
- Catalog evidence: Added real built-in `model.list` coverage for Claude Opus 5.5 (1M context / 128K output). Adaptive-thinking request serialization and Anthropic wire/auth refresh remain.
- Validation: Downloaded official Node 22.22.0 Darwin arm64 archive in `/tmp`, verified the official SHASUMS256 entry, and confirmed its native Rolldown binding loads; this avoids the local signed Node/ad-hoc addon mismatch without changing project Node or security. New focused Node 22 Vitest set: context/import/settlement tests pass; image profile test passes; Gateway context-window/catalog suite 4/4 passes. CortexKit exact package smoke ran through Pi's `jiti` loader under Node 22; normalized prompt/tools survive its Anthropic converter, and startup uses `PI_CODING_AGENT_DIR` with no sidecar creation or live auth/network.
- CortexKit: Exact published `@cortexkit/pi-anthropic-auth@1.23.0` matches npm latest and declares Pi peers `>=0.86.1`; its registration overrides built-in `anthropic` and uses standard Pi OAuth credential hooks. Disposable no-credential fixture confirms no `PI_AGENT_DIR` fallback when Tron sets `PI_CODING_AGENT_DIR`. Relay, fallback accounts/API routes, cache warm/prime, dump and fast mode remain disabled by default and were not enabled. Actual OAuth login/refresh, Anthropic endpoint requests, duplicate global+project registration and scoped Claustrum connection are not tested; installation remains separately gated.
- Remaining: Finish applicable release-delta/media/auth audit, prescribed rollback, full Gateway/Mac/iOS/package validation, and independent review. Full Gateway run previously had four unresolved failures; focused diagnosis showed two message-count expectations are now corrected by excluding non-visible `system` transcript deltas in both live and cold catalog paths, and the two terminal-notification fixtures needed the SDK 0.87 boundary migration described below. Per parent direction the full suite was not rerun; four old full-run failures remain release blockers until revalidated after owner changes. Candidate remains isolated and unmerged; no install into `~/.tron/agent` and no Gateway transition.

### SDK-5 · Candidate checkpoint · 2026-09-23 · worker

- Owner validation: Three complete affected Vitest files passed (RuntimeRegistry integration, catalog metadata index, terminal notifications): 274/274. After a type-only correction for catalog index version 3, Gateway TypeScript build passed and catalog metadata tests passed 15/15. The initial build attempt caught a stale `version: 2` type and failed; the type now follows `typeof CATALOG_METADATA_INDEX_VERSION`. `git diff --check` and the full personal-info guard passed. The final official Node 22 Gateway suite passed 1886/1886 across 174 files.
- Counts/cache: Pi 0.87 writes system-role transcript deltas as provider-context state; they are canonical but not user-visible conversation rows. Live and cold catalog counts now both exclude only `system`, retaining prior count behavior for malformed/non-system entries. Catalog index version is 3; old v2 cache is disposable and will rebuild from canonical JSONL, not migrate canonical sessions.
- Settlement evidence and disposition: SDK package docs explicitly state `agent_before_settle` can append entries and request one continuation, while `agent_settled` fires after automatic work has finished and is notification-only. Type declarations expose `continue` on `BoundaryResult` for `turn_end`/`agent_before_settle`, not `agent_settled`. Test instrumentation records two agent start/end pairs followed by one settled callback for the in-run continuation; one final error alert uses the final error assistant entry, while the original user invocation keeps its completed terminal receipt. A drain-rejected continuation emits a stopped alert against the preceding successful assistant and keeps that receipt completed. `sendMessage(..., { triggerTurn: true })` remains a supported separate trigger from `agent_settled`, but starts a distinct run and has different receipt/notification semantics. Old extensions may continue to trigger there, but this is not parity with an in-run continuation.
- Installed-extension review: Bounded review found no equivalent old-style continuation consumer: `pi-subagents@0.59` uses `agent_settled` to resume widgets (its `triggerTurn` use is under `session_compact`); `pi-web-access@0.22` triggers from async fetch/command paths; no `agent_settled` consumers were found in pi-goal, browser, or pi-sub-anthropic. Arbitrary project extensions were not exhaustively inspected; review any present project-owned extensions before adoption. No compatibility shim or plugin source change was added.
- Full Gateway validation: Official Node 22.22.0 full Vitest run passed 1886/1886 tests across 174 files, including the previously failing message-count and terminal-continuation/drain owners. The earlier four focused failures are resolved; do not retain them as blockers.
- Independent review reconciliation: The reported `parentSession` concern is retracted. Pi's header value is provenance to the import source path, not a reopen/context requirement; RuntimeRegistry tolerates an absent parent, the catalog associates only present sessions, and `context-edit.integration.test.ts` removes the uploaded source then reopens the imported session and proves context remains usable. Keep the documented provenance limitation; do not rewrite headers or archive data. A real `session.import` RPC lease-cleanup/searchability test is a future test opportunity, not a release blocker and not part of this checkpoint. Reviewer found no other candidate-specific code blocker.
- Provider/media evidence: A bounded Node 22 fake-credential/no-network Anthropic `fetch` capture for Opus 5.5 asserted adaptive `thinking`, high effort, system prompt, tool schema, image forwarding, and the model max-token cap. `gateway-context-window.test.ts` covers `model.list` capacity. A temporary RuntimeRegistry/faux-provider fixture proved local `read` tool images take the active 1,000 px profile while the source stays 1,320×2,868; the committed prompt-image regression proves caller bytes are unchanged and canonical history is resized once. No live OAuth or provider request was made.
- Final platform/package validation: iOS test build and `SessionHistoryStoreTests` passed (9/9); `scripts/tron mac generate` and Mac `build-for-testing` passed with existing warnings. The first `bundle-gateway.sh --verify-only` failure was an existing stale dev payload: manifest `channel=dev`, `sourceRevision=42b795f17`, while this candidate expected stable/current `7a4c694a2`; canonical source Push config validated for stable. Running the canonical `bundle-gateway.sh` stage (no allow-empty override) rebuilt from candidate lock/source and staged Node 22.22.0 payload with fingerprint `41576e85db5e42d075eb39d9125b2e4c0f1e5a71edba39c7e0e8b232483cc966`; subsequent `--verify-only` and `test-gateway-payload-verifier.sh` passed. Install reported one high-severity npm audit advisory in production dependencies; not investigated in this validation-only task and must be tracked by the maintainer.
- Remaining gates and dispositions: no deterministic Anthropic adaptive-thinking or per-model prompt/tool image evidence remains outstanding. Actual OAuth login/refresh/cancellation and device/account acceptance are manual adoption checks, not a core SDK migration test claim; CortexKit live installation waits until the manual runtime adoption action. `pi-sub-anthropic@0.1.4` remains incompatible with TranscriptContext and requires a separate replacement decision if still selected. Optional cache warming, fallback routes/relay and other new product surfaces remain explicitly deferred in the matrix. Independent review completed with no candidate-specific code blockers after the parent-path concern was retracted. No merge, Gateway transition, live credentials, extension install, physical-device install, or second canonical runtime client was used.

### SDK-3 · In progress · 2026-09-23 · worker checkpoint

- Result: Candidate pins 0.87.1 and adapts Tron-owned TranscriptContext compaction wrapping, canonical context-edit/system-message history and tree projection, SDK extension-event classification, theme keys, and Opus 5.5 metadata coverage. No upgrade/parity completion is claimed.
- Decision: User approved earlier `phase=compacting` when Pi emits `compaction_start` before summary auth. Trace: 0.84.4 awaited auth before emitting `compaction_start`; 0.87.1 emits `compaction_start`, creates its controller, then awaits auth with an AbortSignal. Gateway projection now truthfully marks active compaction. Tests use an abort-aware mock for supported cancellation and retain a deliberately non-cooperative held-auth case whose operation remains visible/tracked until the barrier is released.
- Evidence: Node 22.22 Gateway build and Pi context/model smoke passed; SDK helper suite passed (27/27); focused Gateway suites passed (129 tests); iOS test build and `SessionHistoryStoreTests` passed (9 tests). Node 22 Vitest could not load Rolldown native binding: project Node is TeamIdentifier `MYGKXH6TY4`, while its package addon is ad-hoc signed. Node 25 focused tests passed but do not substitute for Node 22; Node 22 runtime smoke did pass.
- External package: `pi-sub-anthropic@0.1.4` npm tarball copied into an isolated `/tmp` fixture; TypeScript structural check passes under SDK 0.87.1, but captured Anthropic request lacks both `system` and `tools` because extension still reads removed `context.systemPrompt/tools`. No installed extension, credentials or network were used.
- Remaining: Prove context-edit restart/resume/fork/import/export and exact branch-relative provider contexts; exercise extension settlement/callback ordering, Gateway catalog RPC and Anthropic wire/media behavior; assess per-model image limits; complete rollback/full cross-module checks and independent review. Upgrade task remains active.
- Candidate: `fix/pi-sdk-0871-upgrade` at `/private/tmp/tron-pi-sdk-0871-upgrade`; no Gateway transition and no source merge to main.


### SDK-1 · Done · 2026-09-23 · worker

- Result: Verified npm latest remains 0.87.1 and created the independent candidate worktree after explicit user authorization.
- Evidence: `npm view @earendil-works/pi-coding-agent dist-tags.latest version gitHead engines.node --json` reported 0.87.1, upstream `f07218c4d4bbc12bef056a7058c3dd49dfe41abe`, Node `>=22.19.0`; independent `npm ci` passed under Node 22.22.0.
- Changes: Plan creation/activation/claim metadata on main; isolated branch `fix/pi-sdk-0871-upgrade` at `/private/tmp/tron-pi-sdk-0871-upgrade`.
- Tasks added: SDK-1 through SDK-5.
- Kept on purpose: The unrelated abandoned-login plan remains unmodified; no Gateway lifecycle operation.
- Deviations: Exact update helper preflight/install reached coherence validation then safely restored manifests/tree because current verifier requires removed pi-client/pi-protocol lock entries. Candidate manifests show coding-agent now requires chord; SDK-2 must update only the proven family graph assumptions before retrying helper.
- For the next agent: Continue SDK-2 in isolated worktree. Keep only plan metadata on main; use plan branch update for SDK-2 source commit.
